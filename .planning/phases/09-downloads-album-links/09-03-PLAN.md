---
phase: 09-downloads-album-links
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/Lightbox.jsx
  - site/tests/Lightbox.test.jsx
autonomous: true
requirements:
  - DWNL-01
tags:
  - react
  - lightbox
  - download

must_haves:
  truths:
    - "LightboxWrapper registers the yarl Download plugin"
    - "Each slide passed to YarlLightbox includes a `download` field referencing the photo's r2_url and filename"
    - "Opening the lightbox renders YarlLightbox with `plugins` containing the Download plugin reference"
    - "Existing GALL-03 and GALL-04 lightbox tests continue to pass (filtered subset, correct index)"
  artifacts:
    - path: "site/src/components/Lightbox.jsx"
      provides: "Lightbox with download plugin + per-slide download metadata"
      contains: "plugins"
    - path: "site/tests/Lightbox.test.jsx"
      provides: "Updated tests covering DWNL-01 (download plugin registered, per-slide download fields)"
  key_links:
    - from: "site/src/components/Lightbox.jsx"
      to: "yet-another-react-lightbox/plugins/download"
      via: "import Download from 'yet-another-react-lightbox/plugins/download'"
      pattern: "yet-another-react-lightbox/plugins/download"
    - from: "yarl slide"
      to: "photo.r2_url + photo.filename"
      via: "download: { url, filename } field on each slide"
      pattern: "download:"
---

<objective>
Add the yarl (yet-another-react-lightbox) Download plugin to `LightboxWrapper` and extend each slide with `download` metadata so guests can save the currently viewed photo from the lightbox toolbar. Satisfies DWNL-01.

Purpose: Guests already use the lightbox for full-screen viewing — placing the download action in the lightbox toolbar is the most discoverable path for the "I want this photo" gesture.

Output: `site/src/components/Lightbox.jsx` updated to register the Download plugin and include per-slide download fields; `site/tests/Lightbox.test.jsx` extended with DWNL-01 assertions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-downloads-album-links/09-UI-SPEC.md

<interfaces>
<!-- Current Lightbox.jsx (do not break existing behavior) -->
From site/src/components/Lightbox.jsx (current state):
- default export: LightboxWrapper({ open, index, slides, onClose })
- props.slides = flatFilteredPhotos array (each photo has r2_url, id, filename, …)
- internally maps to yarlSlides: { src: photo.r2_url, alt: photo.id }
- renders <YarlLightbox open={open} close={onClose} slides={yarlSlides} index={index} portal={{ container: { dir: 'rtl' } }} />

<!-- yarl Download plugin contract (from node_modules/yet-another-react-lightbox/dist/plugins/download/index.d.ts) -->
Import:
- import Download from 'yet-another-react-lightbox/plugins/download'

Slide.download field shape (current API; downloadUrl/downloadFilename are @deprecated):
- download?: boolean | string | { url: string; filename: string }
- Use the object form: download: { url: photo.r2_url, filename: photo.filename }

LightboxProps.plugins:
- plugins: Plugin[] — pass [Download]

Toolbar button is appended automatically; no Render override needed.
Hebrew label: yarl exposes labels.Download — optionally pass labels={{ Download: 'הורדה' }} for Hebrew. UI-SPEC §6 leaves this optional but recommends a Hebrew label.

<!-- Existing Lightbox tests (must continue to pass) -->
From site/tests/Lightbox.test.jsx:
- The yarl default export is mocked: vi.mock('yet-another-react-lightbox', () => ({ default: vi.fn(() => null) }))
- Tests inspect yarlMock.mock.calls[last][0] for { open, index, slides }
- L1 expects slides shape { src, alt } — this plan EXTENDS that shape with `download` so the existing equality assertion must be updated to use `expect.arrayContaining` / object-with-download or to assert each slide field individually
- L1 and L2 must keep passing for GALL-03 and GALL-04

<!-- The Download plugin module also needs to be mocked in tests (it has CSS-like side effects through yarl internals) -->
Mock pattern: vi.mock('yet-another-react-lightbox/plugins/download', () => ({ default: 'DownloadPluginMock' }))
Then assert: lastCall[0].plugins includes 'DownloadPluginMock'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Lightbox tests + register Download plugin with per-slide download metadata</name>
  <files>site/src/components/Lightbox.jsx, site/tests/Lightbox.test.jsx</files>
  <read_first>
    - site/src/components/Lightbox.jsx (current 21-line implementation — preserve open/close/index/portal behavior)
    - site/tests/Lightbox.test.jsx (full file — must update L1 slide-shape expectation and add DWNL-01 cases; preserve L1/L2 GALL-03/GALL-04 coverage)
    - site/node_modules/yet-another-react-lightbox/dist/plugins/download/index.d.ts (download field shape, plugin entry point)
    - .planning/phases/09-downloads-album-links/09-UI-SPEC.md §"Modified: Lightbox (LightboxWrapper)" — note UI-SPEC mentions deprecated downloadUrl/downloadFilename; use the modern `download: { url, filename }` form instead per the plugin's current type definitions
    - site/src/hooks/useFilters.js (so test fixtures' filtered set stays consistent)
  </read_first>
  <behavior>
    - Test (DWNL-01a): YarlLightbox is rendered with a `plugins` prop that is an array including the Download plugin module's default export
    - Test (DWNL-01b): every slide passed to YarlLightbox has a `download` field equal to `{ url: photo.r2_url, filename: photo.filename }`
    - Test (DWNL-01c): every slide still has `src === photo.r2_url` and `alt === photo.id` (existing shape preserved)
    - Test (regression L1 GALL-03): clicking the first photo still calls YarlLightbox with open=true and index=0
    - Test (regression L2 GALL-04): with the abir_sultan filter active, slides.length === 3 and index for a2 is 1
  </behavior>
  <action>
    Modify `site/src/components/Lightbox.jsx`: add `import Download from 'yet-another-react-lightbox/plugins/download'` at the top alongside the existing yarl imports. Extend the `yarlSlides` map so each slide object is `{ src: photo.r2_url, alt: photo.id, download: { url: photo.r2_url, filename: photo.filename } }`. Pass `plugins={[Download]}` as a new prop to `<YarlLightbox …/>`. Add `labels={{ Download: 'הורדה' }}` as a new prop on YarlLightbox for the Hebrew toolbar tooltip. Preserve `open`, `close`, `slides`, `index`, and `portal={{ container: { dir: 'rtl' } }}` exactly. Update `site/tests/Lightbox.test.jsx`: add a top-level `vi.mock('yet-another-react-lightbox/plugins/download', () => ({ default: 'DownloadPluginMock' }))` next to the existing yarl mocks. Update the L1 `expectedSlides` assertion to include the `download` field for each slide (e.g. `{ src: 'https://r2.example.com/photos/a1.jpg', alt: 'a1', download: { url: 'https://r2.example.com/photos/a1.jpg', filename: 'a1.jpg' } }`). Add a new `describe('Lightbox (DWNL-01)')` block with three tests: D1 — when the lightbox opens, `lastCall[0].plugins` is an array containing `'DownloadPluginMock'`; D2 — every slide has `download.url === photo.r2_url` and `download.filename === photo.filename`; D3 — `lastCall[0].labels.Download === 'הורדה'`. Keep `import { vi } from 'vitest'` at the top and the existing yarl mock factory unchanged.
  </action>
  <verify>
    <automated>cd site && npm test -- --run tests/Lightbox.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import Download from 'yet-another-react-lightbox/plugins/download'" site/src/components/Lightbox.jsx` returns a match
    - `grep -n "plugins={\\[Download\\]}" site/src/components/Lightbox.jsx` returns a match (with React JSX brace+bracket syntax)
    - `grep -n "download:" site/src/components/Lightbox.jsx` returns a match inside the yarlSlides map (per-slide field)
    - `grep -F "labels={{ Download: 'הורדה' }}" site/src/components/Lightbox.jsx` returns a match
    - `grep -n "yet-another-react-lightbox/plugins/download" site/tests/Lightbox.test.jsx` returns at least one match (the new vi.mock)
    - `grep -n "DWNL-01" site/tests/Lightbox.test.jsx` returns at least one match (new describe block)
    - `cd site && npm test -- --run tests/Lightbox.test.jsx` reports BOTH the original L1+L2 (GALL-03/GALL-04) tests AND the new D1/D2/D3 (DWNL-01) tests passing
    - `cd site && npm test -- --run` full suite remains green
  </acceptance_criteria>
  <done>
    LightboxWrapper registers the yarl Download plugin, each slide carries `{ download: { url, filename } }`, the Hebrew toolbar label "הורדה" is set, and Lightbox.test.jsx covers both DWNL-01 and the existing GALL-03/GALL-04 coverage. DWNL-01 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest browser → R2 (image asset) | yarl's Download plugin fetches photo.r2_url and triggers a save-as via the browser; same trust surface as PhotoCard's download anchor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | Supply-chain integrity of yarl Download plugin | mitigate | Plugin is shipped from the already-installed yet-another-react-lightbox ^3.32.0 package (UI-SPEC §Registry Safety — first-party, no separate install). Lockfile pin maintained. |
| T-09-08 | Information Disclosure | Download metadata leaks via slide objects | accept | download.url and download.filename are already exposed via PhotoCard and metadata.json — no new disclosure |
| T-09-09 | Tampering | XSS via filename in Content-Disposition | mitigate | filename is sourced from pipeline-generated metadata.json (server-controlled), not user input; yarl passes it through saveAs which uses browser-native download semantics |
</threat_model>

<verification>
- `cd site && npm test -- --run tests/Lightbox.test.jsx` → L1, L2 (GALL-03, GALL-04) and D1, D2, D3 (DWNL-01) all pass
- `cd site && npm test -- --run` → full suite (32 existing + new ALBUM_LINKS + new TopBar + new PhotoCard + new DWNL-01) green
- `cd site && npm run build` → no build errors importing the Download plugin
- Manual (post-execute): open any photo in the lightbox → toolbar shows a download button labeled "הורדה" → clicking it downloads the current photo with the original filename
</verification>

<success_criteria>
- yarl Download plugin imported and passed via `plugins={[Download]}` (DWNL-01)
- Every slide has `download: { url: photo.r2_url, filename: photo.filename }`
- Hebrew toolbar label `'הורדה'` set via `labels.Download`
- Existing GALL-03/GALL-04 tests still pass
- New DWNL-01 tests pass
- Full vitest suite green
</success_criteria>

<output>
Create `.planning/phases/09-downloads-album-links/09-03-SUMMARY.md` when done. Include: files modified, plugin integration notes, test results, any UI-SPEC deviations (e.g. using modern `download: { url, filename }` rather than the deprecated `downloadUrl`/`downloadFilename` mentioned in UI-SPEC).
</output>
