---
phase: 02-pipeline-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - pipeline/config.yaml
  - pipeline/tests/__init__.py
  - pipeline/tests/conftest.py
  - pipeline/tests/test_ingest.py
  - pipeline/tests/test_embed.py
  - pipeline/tests/test_cluster.py
  - pipeline/tests/test_resize.py
  - .gitignore
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-06
  - PIPE-07

must_haves:
  truths:
    - "Running `uv run pytest pipeline/tests/ -v` discovers and executes tests for ingest, embed, cluster, and resize modules"
    - "Tests fail (RED) until their corresponding pipeline scripts are implemented in plans 02–05"
    - "pipeline/config.yaml contains an `events.time_windows` block under `pipeline:` with 5 clusters (prep, photoshooting, dining, hupa, dancing) and Israel-local start/end times"
    - "open-clip-torch, scikit-learn, and pytest are installed and importable via `uv run python -c 'import open_clip, sklearn, pytest'`"
    - ".gitignore excludes pipeline/output/ so generated artifacts are not committed"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependency manifest with open-clip-torch, scikit-learn (runtime) and pytest (dev)"
      contains: "open-clip-torch"
    - path: "pipeline/config.yaml"
      provides: "Event time windows for cluster assignment (PIPE-04) under pipeline.events.time_windows"
      contains: "time_windows"
    - path: "pipeline/tests/conftest.py"
      provides: "Shared pytest fixtures: tiny_jpeg, fixture_embeddings, fixture_config"
      exports: ["tiny_jpeg", "fixture_embeddings", "fixture_config"]
    - path: "pipeline/tests/test_ingest.py"
      provides: "RED tests for PIPE-01 (test_exif_normalization, test_zero_offset_treated_as_local) and PIPE-02 (test_photographer_label)"
      contains: "def test_exif_normalization"
    - path: "pipeline/tests/test_embed.py"
      provides: "RED tests for PIPE-03 (test_embedding_shape_normalized) — mocks open_clip"
      contains: "def test_embedding_shape_normalized"
    - path: "pipeline/tests/test_cluster.py"
      provides: "RED tests for PIPE-04, PIPE-05, PIPE-06"
      contains: "def test_time_window_assignment"
    - path: "pipeline/tests/test_resize.py"
      provides: "RED tests for PIPE-07 (web max dim, thumb max dim, orientation correction)"
      contains: "def test_web_image_max_dimension"
  key_links:
    - from: "pipeline/tests/conftest.py"
      to: "pipeline/tests/test_*.py"
      via: "pytest fixture injection"
      pattern: "tiny_jpeg|fixture_embeddings|fixture_config"
    - from: "pipeline/config.yaml"
      to: "future pipeline/cluster.py"
      via: "yaml.safe_load — pipeline.events.time_windows"
      pattern: "events:\\s*\\n\\s*time_windows"
---

<objective>
Wave 0 foundation for Phase 2. Install the ML and test dependencies, scaffold the pytest tree with shared fixtures, write RED test stubs for every PIPE-0X requirement, and extend pipeline/config.yaml with the event time windows that cluster.py will consume.

Purpose: Establish the validation harness before any pipeline script is written. Per 02-VALIDATION.md the test suite must exist first so every later plan can claim its task complete by turning a specific test from RED to GREEN. This plan is the only place where pyproject.toml, .gitignore, config.yaml events block, and the tests/ directory are touched — plans 02–05 only add implementation code.

Output: Installed dependencies (open-clip-torch, scikit-learn, pytest), pipeline/tests/ directory with 4 test files and conftest fixtures, config.yaml events block, .gitignore entries for pipeline/output/.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pipeline-processing/02-RESEARCH.md
@.planning/phases/02-pipeline-processing/02-PATTERNS.md
@.planning/phases/02-pipeline-processing/02-VALIDATION.md
@pipeline/config.yaml
@pipeline/acquire_google.py
@CLAUDE.md

<interfaces>
<!-- Test fixtures established in this plan and consumed by all test files. -->
<!--                                                                         -->
<!-- conftest.py exports (pytest fixtures):                                   -->
<!--   tiny_jpeg(tmp_path) -> Path           — 10x10 white JPEG, no EXIF      -->
<!--   fixture_embeddings() -> np.ndarray    — (5, 512) L2-normalized float32 -->
<!--   fixture_config() -> dict              — minimal config dict matching   -->
<!--                                            pipeline.events.time_windows  -->
<!--                                                                         -->
<!-- Test file -> implementation file mapping (used by plans 02–05):          -->
<!--   test_ingest.py   imports from pipeline.ingest                          -->
<!--   test_embed.py    imports from pipeline.embed (mocks open_clip)         -->
<!--   test_cluster.py  imports from pipeline.cluster                         -->
<!--   test_resize.py   imports from pipeline.resize                          -->
<!--                                                                         -->
<!-- Cluster names (canonical, used across all plans):                        -->
<!--   prep, photoshooting, dining, hupa, dancing                             -->
<!--   NOTE: REQUIREMENTS.md and RESEARCH.md use "dancing" (NOT "party").     -->
<!--   wedding_album_spec / CLAUDE.md mention "party" — the canonical key set -->
<!--   for Phase 2 is the REQUIREMENTS.md list. Phase 4 maps these to Hebrew. -->
<!--                                                                         -->
<!-- Event time windows (Israel local time, GMT+02:00):                       -->
<!--   prep           08:00 - 14:00                                           -->
<!--   photoshooting  14:00 - 16:10                                           -->
<!--   dining         16:10 - 18:00                                           -->
<!--   hupa           18:00 - 18:40                                           -->
<!--   dancing        18:40 - 23:59                                           -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and extend config.yaml</name>
  <files>pyproject.toml, pipeline/config.yaml, .gitignore</files>
  <read_first>
    - pyproject.toml (see current dependencies and dev-dependencies)
    - pipeline/config.yaml (preserve existing sources/photographers/pipeline blocks; add events block under `pipeline:`)
    - .gitignore (see existing patterns; append pipeline/output entry if not present)
    - .planning/phases/02-pipeline-processing/02-RESEARCH.md (Standard Stack, Installation section, and "Code Examples → Config Extension")
  </read_first>
  <action>
    Run `uv add open-clip-torch scikit-learn` then `uv add --dev pytest`. Do NOT pin torch or torchvision separately — `open-clip-torch==3.3.0` will pull torch 2.12.0 and torchvision 0.27.0 transitively. Wait for resolution even if it takes several minutes (per RESEARCH.md Pitfall 7).

    After `uv add` succeeds, edit pipeline/config.yaml: under the existing top-level `pipeline:` key (which currently only has `confidence_threshold: 0.7`), add a sibling `events:` mapping with a `time_windows:` child containing the five clusters in this exact order: `prep`, `photoshooting`, `dining`, `hupa`, `dancing`. Each cluster maps to `{start, end}` quoted ISO-8601 time strings using the values listed in the `<interfaces>` block above. Cluster keys are the canonical names from REQUIREMENTS.md PIPE-04 — use `dancing`, NOT `party`.

    Append `pipeline/output/` to .gitignore (and `__pycache__/` if not already present). Verify pyproject.toml now lists `open-clip-torch` and `scikit-learn` under `[project] dependencies` and `pytest` under `[dependency-groups] dev` (or equivalent uv-managed section).
  </action>
  <verify>
    <automated>uv run python -c "import open_clip, sklearn, numpy, torch, pytest; print('ok')" && uv run python -c "import yaml,sys; c=yaml.safe_load(open('pipeline/config.yaml')); tw=c['pipeline']['events']['time_windows']; assert list(tw.keys())==['prep','photoshooting','dining','hupa','dancing'], tw; assert tw['hupa']=={'start':'18:00','end':'18:40'}; print('config-ok')" && grep -q '^pipeline/output/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import open_clip"` exits 0
    - `uv run python -c "import sklearn"` exits 0
    - `uv run python -c "import pytest"` exits 0
    - pyproject.toml contains the string `open-clip-torch` and the string `scikit-learn`
    - pipeline/config.yaml `pipeline.events.time_windows` has exactly the 5 keys `prep, photoshooting, dining, hupa, dancing`
    - `pipeline.events.time_windows.hupa.start == "18:00"` and `.end == "18:40"`
    - .gitignore contains a line matching `^pipeline/output/`
    - All existing keys in pipeline/config.yaml (sources, photographers, pipeline.confidence_threshold) remain unchanged
  </acceptance_criteria>
  <done>Dependencies installed, config.yaml extended with events block, .gitignore updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold pipeline/tests/ with shared fixtures</name>
  <files>pipeline/tests/__init__.py, pipeline/tests/conftest.py</files>
  <read_first>
    - .planning/phases/02-pipeline-processing/02-PATTERNS.md (section "pipeline/tests/conftest.py (test fixtures — no analog)")
    - .planning/phases/02-pipeline-processing/02-RESEARCH.md (Validation Architecture section)
    - pipeline/config.yaml (final state after Task 1 — fixture_config must mirror this structure)
  </read_first>
  <behavior>
    - Test 1: `tiny_jpeg` fixture yields a Path to a 10x10 white JPEG with no EXIF
    - Test 2: `fixture_embeddings` returns numpy ndarray of shape (5, 512), dtype float32, each row L2-normalized to ≈ 1.0
    - Test 3: `fixture_config` returns a dict whose `pipeline.events.time_windows` matches the canonical 5-cluster mapping
  </behavior>
  <action>
    Create empty `pipeline/tests/__init__.py` (file may be zero bytes).

    Create `pipeline/tests/conftest.py` following the exact pattern in 02-PATTERNS.md "conftest.py" section. Define three `@pytest.fixture()` functions:
      - `tiny_jpeg(tmp_path)` — uses `PIL.Image.new("RGB", (10, 10), color=(255, 255, 255))`, saves as JPEG to `tmp_path / "fixture.jpg"`, returns the Path.
      - `fixture_embeddings()` — uses `np.random.default_rng(42)` (seeded for determinism) to create a `(5, 512)` array, casts to `float32`, L2-normalizes each row by dividing by `np.linalg.norm(arr, axis=1, keepdims=True)`.
      - `fixture_config()` — returns a dict literal whose `pipeline.events.time_windows` contains all 5 clusters with the same start/end values as pipeline/config.yaml; include `pipeline.confidence_threshold: 0.7`.

    Module docstring on conftest.py: `"""Shared pytest fixtures for pipeline tests."""`. Use `from __future__ import annotations`. Imports: `from pathlib import Path`, `import numpy as np`, `import pytest`, `from PIL import Image`.
  </action>
  <verify>
    <automated>uv run pytest pipeline/tests/ --collect-only -q 2>&1 | grep -E "(no tests ran|collected)" && uv run python -c "from pipeline.tests.conftest import *; print('importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/tests/__init__.py` exists (file present, may be empty)
    - `pipeline/tests/conftest.py` contains `def tiny_jpeg`, `def fixture_embeddings`, `def fixture_config`
    - All three functions are decorated with `@pytest.fixture` or `@pytest.fixture()`
    - `uv run pytest pipeline/tests/ --collect-only` exits 0 (collection succeeds even with no tests yet)
    - conftest.py uses `from __future__ import annotations`
  </acceptance_criteria>
  <done>Pytest tree initialized; fixtures importable and usable by all downstream test files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED test stubs for ingest, embed, cluster, resize</name>
  <files>pipeline/tests/test_ingest.py, pipeline/tests/test_embed.py, pipeline/tests/test_cluster.py, pipeline/tests/test_resize.py</files>
  <read_first>
    - .planning/phases/02-pipeline-processing/02-VALIDATION.md (Per-Task Verification Map — the test function names listed there are non-negotiable)
    - .planning/phases/02-pipeline-processing/02-RESEARCH.md (Phase Requirements → Test Map; Common Pitfalls #1, #3 for EXIF cases to cover)
    - .planning/phases/02-pipeline-processing/02-PATTERNS.md (sections "test_ingest.py" and "test_embed.py")
    - pipeline/tests/conftest.py (final state from Task 2 — see fixture names)
  </read_first>
  <behavior>
    - All test files use `from __future__ import annotations` and pytest test functions (no class wrappers required).
    - Tests import functions from `pipeline.ingest`, `pipeline.embed`, `pipeline.cluster`, `pipeline.resize`. These modules do NOT yet exist — tests must fail with ImportError or similar in this plan and turn GREEN as plans 02–05 implement the modules.
    - Each test asserts a single behavior listed in 02-VALIDATION.md "Per-Task Verification Map".
    - test_embed.py uses `unittest.mock.patch` to replace `open_clip.create_model_and_transforms` with a MagicMock returning a deterministic embedding — the real model must NEVER load during tests.
  </behavior>
  <action>
    Create the four test files with the exact test function names from 02-VALIDATION.md:

    `pipeline/tests/test_ingest.py` — module docstring `"""Unit tests for pipeline/ingest.py — covers PIPE-01, PIPE-02."""`. Test functions (each can start as a one-line `pytest.fail("not yet implemented")` stub OR a full assertion using the fixture — full assertions are preferred because they will turn GREEN automatically when Plan 02 ships ingest.py):
      - `test_exif_normalization(tmp_path)` — write a fixture JPEG with `DateTimeOriginal="2026:05:01 18:30:00"` and `OffsetTimeOriginal="+02:00"` via `PIL` `exif` bytes (use `Image.Exif()` API), then call `extract_timestamp(path)` and assert the returned datetime equals `datetime(2026,5,1,18,30,0,tzinfo=timezone(timedelta(hours=2)))`.
      - `test_zero_offset_treated_as_local(tmp_path)` — write JPEG with `DateTimeOriginal="2026:05:01 18:30:00"` and `OffsetTimeOriginal="+00:00"`; assert `extract_timestamp(path).hour == 18` and tzinfo offset is `timedelta(hours=2)` (i.e. +00:00 is interpreted as local Israel time, NOT UTC — per RESEARCH.md Pitfall #1).
      - `test_photographer_label(fixture_config)` — call `assign_photographer_label("sources/abir_sultan/IMG.JPG", config_sources_list)` with a list mirroring `config["sources"]["local_sources"]`; assert returned label is `"abir_sultan"`.

    `pipeline/tests/test_embed.py` — module docstring `"""Unit tests for pipeline/embed.py — covers PIPE-03."""`. Test function:
      - `test_embedding_shape_normalized(tiny_jpeg, monkeypatch)` — patch `open_clip.create_model_and_transforms` to return `(MagicMock_model, None, lambda img: torch.zeros(3,224,224))`; configure the mock model's `encode_image` to return a tensor that yields a 512-dim L2-normalized output. Call `embed_image(tiny_jpeg, model, preprocess)` and assert `result.shape == (512,)` and `abs(np.linalg.norm(result) - 1.0) < 1e-4`. Use the conftest `tiny_jpeg` fixture.

    `pipeline/tests/test_cluster.py` — module docstring `"""Unit tests for pipeline/cluster.py — covers PIPE-04, PIPE-05, PIPE-06."""`. Test functions:
      - `test_time_window_assignment(fixture_config)` — for representative datetimes (08:30 → prep, 15:00 → photoshooting, 17:00 → dining, 18:15 → hupa, 20:00 → dancing) call `assign_cluster_by_time(dt, fixture_config["pipeline"]["events"]["time_windows"])` and assert the (cluster, confidence) returned matches expected; confidence must be `1.0` for in-window assignments.
      - `test_knn_assignment(fixture_embeddings)` — split fixture_embeddings into 3 "anchor" rows with labels `["prep","photoshooting","dining"]` and 2 "film" rows; call `compute_centroids` then `assign_film_photos`; assert each film row gets one of the three anchor labels and `0.0 <= confidence <= 1.0`.
      - `test_exif_confidence_is_one(fixture_config)` — `assign_cluster_by_time(datetime(...,18,30,..., tzinfo=ISRAEL_TZ), ...)` returns confidence == 1.0 (PIPE-06).
      - `test_low_confidence_flagged(tmp_path, fixture_config)` — build a small list of photo dicts including one with `cluster_confidence=0.55` (below the 0.7 threshold), call the cluster.py helper that writes `low_confidence.txt` (e.g. `write_low_confidence_report(entries, path, threshold)`), and assert the output file contains the flagged photo_id and a header line beginning with `#`.

    `pipeline/tests/test_resize.py` — module docstring `"""Unit tests for pipeline/resize.py — covers PIPE-07."""`. Test functions:
      - `test_web_image_max_dimension(tmp_path)` — create a 4000x3000 RGB JPEG, call `resize_photo(src, web_out, thumb_out)`, open web_out and assert `max(width, height) <= 2000`.
      - `test_thumb_max_dimension(tmp_path)` — same setup, assert `max(width, height) <= 400` for thumb_out.
      - `test_exif_orientation_corrected(tmp_path)` — create a 100x200 (portrait) image, attach EXIF orientation=6 (rotate 90° CW) via Pillow Exif API, save; call resize_photo; open web_out and assert the orientation tag is absent OR pixel dimensions reflect rotation (i.e. width > height for an originally tagged-as-portrait file that PIL would auto-transpose).

    All tests should use `from __future__ import annotations`, import only stdlib + numpy + PIL + pytest + the (not-yet-existing) `pipeline.*` modules. Do NOT import open_clip directly in test code — always go through mocks.
  </action>
  <verify>
    <automated>uv run pytest pipeline/tests/ --collect-only -q 2>&1 | grep -E "^[0-9]+ tests collected" || uv run pytest pipeline/tests/ --collect-only -q 2>&1 | grep -E "collected [0-9]+ item"</automated>
  </verify>
  <acceptance_criteria>
    - pytest collects exactly these test function names across the 4 files: `test_exif_normalization`, `test_zero_offset_treated_as_local`, `test_photographer_label`, `test_embedding_shape_normalized`, `test_time_window_assignment`, `test_knn_assignment`, `test_exif_confidence_is_one`, `test_low_confidence_flagged`, `test_web_image_max_dimension`, `test_thumb_max_dimension`, `test_exif_orientation_corrected` (11 tests total)
    - `uv run pytest pipeline/tests/ --collect-only` exits 0
    - Running `uv run pytest pipeline/tests/ -x` exits non-zero (RED — expected, because pipeline modules do not exist yet)
    - The error output from running the tests is import-related (e.g. `ModuleNotFoundError: No module named 'pipeline.ingest'`) — NOT syntax errors in the test files themselves
    - Every test file uses `from __future__ import annotations`
    - test_embed.py contains the string `monkeypatch` or `mock.patch` or `unittest.mock` (no real model load)
  </acceptance_criteria>
  <done>All 11 test functions defined and collectable; suite is RED pending plans 02–05 implementations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → local env | `uv add` fetches wheels from PyPI; supply-chain risk on `open-clip-torch` (tagged [ASSUMED] in 02-RESEARCH.md Package Legitimacy Audit) |
| config.yaml → pipeline scripts | User-editable YAML; future ingest.py/resize.py will treat output paths from config as data, not code |
| test fixture filesystem | tmp_path-scoped — no risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | All packages reviewed in 02-RESEARCH.md Package Legitimacy Audit. `open-clip-torch` is tagged [ASSUMED] from mlfoundations (well-known research org, 20k+ GitHub stars). `scikit-learn`, `pytest` are industry-standard [VERIFIED]. slopcheck was unavailable at research time — see T-02-SC-CHECKPOINT. |
| T-02-SC-CHECKPOINT | Tampering | open-clip-torch install | mitigate | Pre-install human verification: confirm `pypi.org/project/open-clip-torch/` shows mlfoundations as maintainer and current version is 3.3.0 (uploaded 2026-02-27 or later). Reject install if registry data differs from RESEARCH.md. |
| T-02-01 | Tampering | pipeline/config.yaml events.time_windows | accept | Config is committed to git and reviewed in PRs. Values constrain wedding-day clusters only; no security boundary. |
| T-02-02 | Information Disclosure | pipeline/output/ in repo | mitigate | .gitignore excludes `pipeline/output/` — generated artifacts (catalog.json, embeddings.npy, metadata.json, low_confidence.txt, photos/, thumbs/) never leave the developer machine until Phase 3 upload. |
</threat_model>

<verification>
After all three tasks complete:
- `uv run python -c "import open_clip, sklearn, pytest, numpy, torch"` exits 0
- `uv run pytest pipeline/tests/ --collect-only -q` reports 11 tests collected, 0 errors
- `uv run pytest pipeline/tests/ -x` exits non-zero with ImportError for `pipeline.ingest` / `pipeline.embed` / `pipeline.cluster` / `pipeline.resize` (RED — expected)
- `git status` shows the 9 modified/created files and nothing under `pipeline/output/`
</verification>

<success_criteria>
- All Phase 2 ML and test dependencies installed and importable
- pipeline/config.yaml extended with `pipeline.events.time_windows` containing all 5 canonical clusters (prep, photoshooting, dining, hupa, dancing) at the exact REQUIREMENTS.md PIPE-04 times
- pipeline/tests/ directory contains conftest.py + 4 test_*.py files with 11 collectable test functions
- Test suite is RED (ImportError on pipeline.ingest/embed/cluster/resize) — the gate that plans 02–05 must turn GREEN one module at a time
- pipeline/output/ entry present in .gitignore
</success_criteria>

<output>
Create `.planning/phases/02-pipeline-processing/02-01-SUMMARY.md` when done. Document:
- Exact pyproject.toml dependency entries added (with versions resolved by uv)
- The fixture function signatures in conftest.py
- The 11 test function names so plans 02–05 can reference them by name
- Confirmation that test suite is RED with the expected ImportError(s)
</output>
