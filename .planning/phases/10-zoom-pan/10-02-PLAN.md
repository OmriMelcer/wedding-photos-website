---
phase: 10-zoom-pan
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
files_modified: []
autonomous: false
requirements:
  - ZOOM-01
  - ZOOM-02
  - ZOOM-03

must_haves:
  truths:
    - "On a real mobile device, pinch-to-zoom expands the lightbox image and pinch-to-shrink collapses it back"
    - "On desktop, scroll-wheel zooms the lightbox image in and out without scrolling the page"
    - "When zoomed in, dragging the image pans to show hidden regions"
    - "After a fast pinch-out on iOS, a horizontal swipe navigates to the next photo (not blocked by residual zoom state)"
    - "RTL toolbar shows Zoom controls on the outer (left) side of the toolbar"
  artifacts: []
  key_links: []
---

<objective>
Manual smoke-test checkpoint: verify that the Zoom plugin works correctly on real devices and in a real browser before Phase 10 is marked complete. Automated tests (10-01) confirm correct configuration values; this plan confirms correct runtime behavior where configuration alone cannot.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run production build check</name>
  <files></files>
  <read_first>
    - site/src/components/Lightbox.jsx (confirm Zoom import, plugins, zoom prop, and labels are present before starting)
    - site/package.json (confirm no new dependencies were added)
  </read_first>
  <behavior>
    - Dev server starts cleanly at http://localhost:5173 (or next available port)
    - `npm run build` in site/ exits 0 with no warnings about unresolved imports
    - `npx vitest run` in site/ exits 0 — all tests including ZOOM-01/02/03 describe block pass
  </behavior>
  <action>
    Run `cd site && npx vitest run` and confirm exit 0. Run `npm run build` and confirm exit 0. Start `npm run dev` and leave the dev server running for the manual checks in Task 2. Print the local URL.
  </action>
  <verify>
    <automated>cd site && npx vitest run 2>&1 | tail -15 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (all tests pass, including the ZOOM-01/02/03 block added in plan 10-01)
    - `npm run build` exits 0 with no import errors mentioning `yet-another-react-lightbox/plugins/zoom`
    - Dev server is accessible at localhost:5173 (or printed port)
  </acceptance_criteria>
</task>

<task type="checkpoint:human">
  <name>Task 2: Manual smoke-test — zoom and pan behavior [BLOCKING CHECKPOINT]</name>
  <files></files>
  <read_first>
    - .planning/REQUIREMENTS.md (exact acceptance criteria for ZOOM-01, ZOOM-02, ZOOM-03)
  </read_first>
  <behavior>
    Smoke-test each requirement against the running dev server. All must pass before Phase 10 is marked complete.
  </behavior>
  <action>
    Open the dev server URL in a browser. Click any photo to open the lightbox. Perform these manual checks:

    **Desktop (ZOOM-02 + ZOOM-03):**
    1. Scroll wheel over the open lightbox photo — confirm image zooms in and out without the page scrolling behind it.
    2. Zoom in, then click-and-drag the image — confirm it pans to reveal hidden regions.
    3. Confirm RTL toolbar shows Zoom controls on the outer-left side, and הורדה (Download) further in.

    **Mobile or DevTools mobile simulation (ZOOM-01 + ZOOM-03):**
    4. Enable mobile device simulation in DevTools (or use a real device at the network URL).
    5. Two-finger spread to zoom in — confirm image zooms in.
    6. Two-finger pinch — confirm image zooms back out.
    7. While zoomed in, one-finger drag — confirm image pans.
    8. Perform a fast pinch-out then immediately swipe horizontally — confirm slide navigation triggers (the swipe is NOT blocked by residual zoom state).

    Report results for each check.
  </action>
  <verify>
    <automated>echo "Manual verification required — autonomous: false"</automated>
  </verify>
  <acceptance_criteria>
    - ZOOM-01: Pinch-to-zoom works on mobile (step 5 and 6 above pass without the image freezing or refusing to zoom)
    - ZOOM-02: Scroll-wheel zooms the image on desktop without scrolling the page (step 1 passes)
    - ZOOM-03: Drag-to-pan works while zoomed in on both desktop (step 2) and mobile (step 7)
    - RTL toolbar order: Zoom controls appear on the outer-left side of the toolbar (step 3)
    - iOS swipe-after-pinch: After fast pinch-out, horizontal swipe navigates slides rather than being blocked (step 8 passes or is not applicable on the test device)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Dev server | Local dev server; no public exposure during smoke test |
| Mobile device ↔ Dev server | Same local network; no data leaves the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SM-01 | Information Disclosure | Dev server on LAN during mobile smoke test | accept | Dev server is on local network only; no auth or real guest data involved; test photos are development fixtures |
</threat_model>

<verification>
Phase 10 is complete when:
- Task 1 automated checks pass (tests + build)
- Task 2 manual checks all pass (or the smoke-test result is reviewed and accepted by the developer)
- No new npm packages were installed during execution
</verification>

<success_criteria>
- All 8 manual checks in Task 2 pass (or explicitly accepted with documented rationale for any that are N/A on the test device)
- `npx vitest run` exits 0 with ZOOM-01/02/03 tests passing
- `npm run build` exits 0
- Phase 10 can be marked complete in STATE.md
</success_criteria>

<output>
After completing manual verification, create `.planning/phases/10-zoom-pan/10-02-SUMMARY.md` with:
- Results of each manual smoke-test step (pass/fail/n-a)
- Device and browser used for mobile test
- Confirmation that Phase 10 is ready to close
</output>
