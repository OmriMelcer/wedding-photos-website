---
phase: 04-react-site
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - site/src/App.jsx
  - site/src/components/Filters.jsx
  - site/src/components/Gallery.jsx
  - site/src/components/GallerySection.jsx
  - site/src/components/PhotoCard.jsx
  - site/src/components/Lightbox.jsx
  - site/src/components/LoadingSkeleton.jsx
  - site/src/components/ErrorState.jsx
  - site/src/components/EmptyState.jsx
  - site/tests/Filters.test.jsx
  - site/tests/Gallery.test.jsx
  - site/tests/Lightbox.test.jsx
autonomous: true
requirements:
  - GALL-02
  - GALL-03
  - GALL-04
  - FILT-04
  - DSGN-01
  - I18N-01
  - I18N-02
tags:
  - components
  - gallery
  - filters
  - lightbox
  - masonry
  - shadcn
  - tailwind
  - rtl
  - hebrew
must_haves:
  truths:
    - "App.jsx wires usePhotos + useFilters and renders Filters + Gallery + Lightbox + Loading/Error states"
    - "Gallery iterates PHASE_ORDER and renders one GallerySection per non-empty phase (D-04, D-05)"
    - "GallerySection shows a sticky Hebrew header from PHASE_LABELS[phase] + photo count, then react-masonry-css grid of PhotoCards"
    - "PhotoCard renders <img src={thumb_url} loading='lazy'> with a hover-only photographer-name badge using PHOTOGRAPHER_NAMES (D-13, D-14)"
    - "Lightbox uses yet-another-react-lightbox with flatFilteredPhotos as slides and respects active filters (GALL-04, D-07)"
    - "Filters bar is sticky-top, renders photographer chips + phase chips + (face filter ONLY when people.length > 0) + clear-all 'הצג הכל' button"
    - "Face filter element is absent from the DOM (not display:none) when people.length === 0 (FILT-04)"
    - "All visible UI text is in Hebrew sourced from @/config or the UI-SPEC §6 copywriting contract — no hard-coded Hebrew literals in components beyond the §6 strings"
    - "All directional padding/margin uses Tailwind logical utilities (ps-, pe-, ms-, me-, start-, end-) — no pl-/pr-/ml-/mr-/right-/left- in component files"
  artifacts:
    - path: "site/src/App.jsx"
      provides: "Root component composing usePhotos + useFilters; renders LoadingSkeleton / ErrorState / Filters+Gallery+Lightbox"
      contains: "usePhotos"
    - path: "site/src/components/Filters.jsx"
      provides: "Sticky filter bar with photographer chips, phase chips, conditional face filter, clear-all"
      exports: ["default"]
    - path: "site/src/components/Gallery.jsx"
      provides: "Iterates PHASE_ORDER; conditionally renders GallerySection per non-empty phase; shows EmptyState when all empty"
      exports: ["default"]
    - path: "site/src/components/GallerySection.jsx"
      provides: "Sticky Hebrew header + react-masonry-css grid of PhotoCards"
      exports: ["default"]
    - path: "site/src/components/PhotoCard.jsx"
      provides: "Lazy-loaded thumbnail + hover-only photographer-name badge"
      exports: ["default"]
    - path: "site/src/components/Lightbox.jsx"
      provides: "yarl wrapper with slides=flatFilteredPhotos, RTL portal config"
      exports: ["default"]
    - path: "site/src/components/LoadingSkeleton.jsx"
      provides: "Animated stone-200 skeleton grid shown while metadata.json is loading"
      exports: ["default"]
    - path: "site/src/components/ErrorState.jsx"
      provides: "Hebrew error card with shadcn Button retry"
      exports: ["default"]
    - path: "site/src/components/EmptyState.jsx"
      provides: "Hebrew empty-results message when all filtered sections are empty"
      exports: ["default"]
    - path: "site/tests/Filters.test.jsx"
      provides: "FILT-04 coverage (face filter absence when people=[]) + chip render + clear-all behavior"
      contains: "people"
    - path: "site/tests/Gallery.test.jsx"
      provides: "GALL-02 coverage: sections rendered in PHASE_ORDER, empty sections absent from DOM"
      contains: "PHASE_ORDER"
    - path: "site/tests/Lightbox.test.jsx"
      provides: "GALL-03 + GALL-04 coverage: opens on photo click, slides prop matches flatFilteredPhotos (filtered, not all)"
      contains: "slides"
  key_links:
    - from: "site/src/App.jsx"
      to: "site/src/hooks/usePhotos.js"
      via: "import { usePhotos } from '@/hooks/usePhotos'"
      pattern: "usePhotos"
    - from: "site/src/App.jsx"
      to: "site/src/hooks/useFilters.js"
      via: "import { useFilters } from '@/hooks/useFilters'"
      pattern: "useFilters"
    - from: "site/src/components/Filters.jsx"
      to: "site/src/config.js"
      via: "import { PHASE_LABELS, PHASE_ORDER, PHOTOGRAPHER_NAMES } from '@/config'"
      pattern: "PHOTOGRAPHER_NAMES"
    - from: "site/src/components/Filters.jsx"
      to: "site/src/components/ui/button.jsx"
      via: "import { Button } from '@/components/ui/button'"
      pattern: "@/components/ui/button"
    - from: "site/src/components/Gallery.jsx"
      to: "site/src/components/GallerySection.jsx"
      via: "PHASE_ORDER.map(phase => sectionPhotos.length > 0 && <GallerySection ... />)"
      pattern: "GallerySection"
    - from: "site/src/components/GallerySection.jsx"
      to: "react-masonry-css"
      via: "import Masonry from 'react-masonry-css'"
      pattern: "react-masonry-css"
    - from: "site/src/components/PhotoCard.jsx"
      to: "site/src/config.js"
      via: "import { PHOTOGRAPHER_NAMES } from '@/config'"
      pattern: "PHOTOGRAPHER_NAMES\\[photo.photographer\\]"
    - from: "site/src/components/Lightbox.jsx"
      to: "yet-another-react-lightbox"
      via: "import Lightbox from 'yet-another-react-lightbox'"
      pattern: "yet-another-react-lightbox"
---

<objective>
Replace the App.jsx stub with the full wired root component and implement all 8 React components (Filters, Gallery, GallerySection, PhotoCard, Lightbox, LoadingSkeleton, ErrorState, EmptyState) per the patterns and UI-SPEC. Wire usePhotos + useFilters together. Add automated tests for FILT-04 (face filter absence), GALL-02 (section rendering against PHASE_ORDER with empty-section DOM removal), and GALL-03 + GALL-04 (lightbox open + slides-equal-flatFiltered-not-all-photos).

Purpose: This plan delivers the user-visible gallery — every Phase 4 requirement that produces pixels lands here. The hooks from Plan 02 are consumed; the config from Plan 01 is consumed; the styling baseline (Tailwind v4 + shadcn + Heebo + RTL) from Plan 01 is consumed. Splitting the 8 components into one plan is intentional: each is ~30–50 lines of patterned JSX, they share the same imports and styling vocabulary, and decomposing further would force redundant context reads in each sub-plan.

Output: A fully working gallery. `cd site && npm run dev` opens a Hebrew-RTL page with the synthetic 15-photo fixture, allowing the user to filter by photographer and phase, click any thumbnail to open the lightbox, and navigate prev/next through the filtered subset. All FILT-04, GALL-02, GALL-03, GALL-04 invariants covered by Vitest tests. DSGN-01, I18N-01, I18N-02 implemented in code and verified visually in Plan 04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-react-site/04-CONTEXT.md
@.planning/phases/04-react-site/04-RESEARCH.md
@.planning/phases/04-react-site/04-PATTERNS.md
@.planning/phases/04-react-site/04-UI-SPEC.md
@.planning/phases/04-react-site/04-01-PLAN.md
@.planning/phases/04-react-site/04-02-PLAN.md
@site/src/config.js
@site/src/hooks/usePhotos.js
@site/src/hooks/useFilters.js
@site/src/components/ui/button.jsx
@site/public/metadata.json

<interfaces>
<!-- Component prop contracts. Every component default-exports a function with these props. -->

App: () => JSX                                       // owns lightboxIndex state; composes hooks
Filters({ people, selectedPhotographers, selectedPhases, onTogglePhotographer, onTogglePhase, onClearAll }): JSX
Gallery({ filteredByPhase, flatFilteredPhotos, onPhotoClick }): JSX
GallerySection({ phase, photos, flatFilteredPhotos, onPhotoClick }): JSX
PhotoCard({ photo, onClick }): JSX
Lightbox({ open, index, slides, onClose }): JSX       // file exports default `LightboxWrapper` but App imports it as Lightbox
LoadingSkeleton(): JSX
ErrorState({ error }): JSX
EmptyState(): JSX

<!-- Hebrew strings from UI-SPEC §6 copywriting contract. -->
Page title: 'האלבום שלנו' (already in index.html)
Group label photographers: 'צלמים'
Group label phases: 'שלבים'
Clear all button: 'הצג הכל'
Photo count suffix: 'תמונות' (used as `(N תמונות)`)
Error heading: 'לא ניתן לטעון את התמונות'
Error body: 'אירעה שגיאה בטעינת האלבום. אפשר לנסות לרענן את הדף.'
Retry button: 'רענן'
Empty state body: 'אין תמונות התואמות לסינון הנוכחי'
Empty state clear link: 'הסר סינון'
Lightbox close aria: 'סגור'
Lightbox prev aria: 'תמונה קודמת'
Lightbox next aria: 'תמונה הבאה'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement App.jsx + Filters.jsx + Gallery.jsx + GallerySection.jsx + PhotoCard.jsx + Lightbox.jsx + supporting state components</name>
  <files>site/src/App.jsx, site/src/components/Filters.jsx, site/src/components/Gallery.jsx, site/src/components/GallerySection.jsx, site/src/components/PhotoCard.jsx, site/src/components/Lightbox.jsx, site/src/components/LoadingSkeleton.jsx, site/src/components/ErrorState.jsx, site/src/components/EmptyState.jsx</files>
  <read_first>
    - .planning/phases/04-react-site/04-PATTERNS.md (every component section: App.jsx, Filters.jsx, Gallery.jsx, GallerySection.jsx, PhotoCard.jsx, Lightbox.jsx, LoadingSkeleton.jsx, ErrorState.jsx, EmptyState.jsx — all canonical shapes)
    - .planning/phases/04-react-site/04-UI-SPEC.md (full file — §2 spacing, §3 typography, §4 colors, §5 component inventory, §6 copywriting, §7 RTL contract, §8 motion, §9 accessibility, §10 performance, §11 file structure)
    - .planning/phases/04-react-site/04-CONTEXT.md (D-03 through D-15 — all UI decisions)
    - .planning/phases/04-react-site/04-RESEARCH.md §Pattern 3 (yarl RTL), §Pattern 4 (masonry breakpoints), §Pitfall 3 (yarl slides = filtered), §Pitfall 4 (section conditional render not CSS hide), §Pitfall 5 (logical properties), §Pitfall 6 (masonry CSS required)
    - site/src/App.jsx (the stub from Plan 01 — being fully replaced)
    - site/src/hooks/usePhotos.js (sibling for return shape)
    - site/src/hooks/useFilters.js (sibling for return shape)
    - site/src/config.js (PHASE_LABELS, PHASE_ORDER, PHOTOGRAPHER_NAMES to import)
    - site/src/components/ui/button.jsx (shadcn Button — props/variant API)
  </read_first>
  <behavior>
    Component-level behaviors (verified visually via dev server; assertion-level behaviors in Task 2 tests):
    - App: loading → LoadingSkeleton; error → ErrorState; success → Filters + Gallery + Lightbox stack
    - Filters: photographer chips render PHOTOGRAPHER_NAMES values (real Hebrew/English names); phase chips render PHASE_LABELS values (Hebrew); active chip = warm-gold accent background; inactive chip = stone-200; face filter wrapper element ONLY in DOM when people.length > 0; 'הצג הכל' button only when hasActiveFilters
    - Gallery: iterates PHASE_ORDER (not Object.keys(filteredByPhase) — order would be lost); sections with 0 photos are not in the DOM; if all sections empty, EmptyState renders
    - GallerySection: sticky header at `top-16` (below filter bar); Hebrew title from PHASE_LABELS[phase] with `(N תמונות)` count; Masonry grid with breakpointCols { default:5, 1280:5, 1024:4, 640:3, 0:2 }; uses CSS classes `masonry-grid` and `masonry-grid_column` (defined in index.css from Plan 01)
    - PhotoCard: `<img src={photo.thumb_url} loading="lazy">` (lazy MANDATORY per UI-SPEC §10); hover badge uses `opacity-0 group-hover:opacity-100`; badge text = `PHOTOGRAPHER_NAMES[photo.photographer] ?? photo.photographer` (fallback to key if name missing); positioned at the visual bottom-right corner per D-13 — use logical `bottom-0 start-0` (in RTL, `start` = visual right)
    - Lightbox: open prop = (lightboxIndex >= 0); slides prop = flatFilteredPhotos mapped to `{ src: photo.r2_url, alt: photo.id }`; index prop = lightboxIndex; portal config `{ container: { dir: 'rtl' } }`
    - LoadingSkeleton: 12 stone-200 cards with `animate-pulse`; varied aspect ratios per UI-SPEC §5.6
    - ErrorState: shadcn `Button` for retry; Hebrew copy per UI-SPEC §5.7
    - EmptyState: Hebrew copy per UI-SPEC §5.8
  </behavior>
  <action>
    Implement all 9 component files using the canonical shapes in 04-PATTERNS.md. Each file's structure is fully specified there — copy the patterns and adapt only where Claude's discretion is allowed (loading skeleton variation, error message tone, hover timing within UI-SPEC §8 range). Specific implementation notes per file:

    1. **site/src/App.jsx** — REPLACE the Plan-01 stub entirely. Use the canonical shape from 04-PATTERNS.md `site/src/App.jsx` section. Imports: `useState` from 'react', usePhotos from `@/hooks/usePhotos`, useFilters from `@/hooks/useFilters`, Filters/Gallery/Lightbox/LoadingSkeleton/ErrorState from `@/components/*`. Owns one piece of UI state: `const [lightboxIndex, setLightboxIndex] = useState(-1)`. Renders LoadingSkeleton when loading, ErrorState when error, otherwise the main layout: `<div className="min-h-screen bg-stone-50">` containing Filters → `<main className="px-4 sm:px-8 lg:px-12">` with Gallery → LightboxWrapper. Imports Lightbox as `LightboxWrapper from '@/components/Lightbox'` (the file's default export name). Passes `open={lightboxIndex >= 0}`, `index={lightboxIndex}`, `slides={flatFilteredPhotos}`, `onClose={() => setLightboxIndex(-1)}`.

    2. **site/src/components/Filters.jsx** — Use the canonical shape from 04-PATTERNS.md `site/src/components/Filters.jsx` section. Sticky `top-0 z-10` bar with `bg-stone-100 border-b border-stone-200 px-4 sm:px-8 lg:px-12 py-3`. Two rendered chip groups: `Object.entries(PHOTOGRAPHER_NAMES).map(...)` for photographers (display the Hebrew name as the chip label), `PHASE_ORDER.map(...)` for phases (display `PHASE_LABELS[phase]` as the chip label). Both use the same chip pattern: `<button onClick={...}>` with `className` conditional on `selectedX.has(key)` toggling between active (warm-gold accent — use the custom CSS variable or `bg-stone-800 text-white` per pattern) and inactive (`bg-stone-200 text-stone-700 hover:bg-stone-300`). Chip min-height 44px (touch target). Conditional render face filter ONLY when `people.length > 0`: `{people.length > 0 && <div data-testid="face-filter">{/* Phase 2 */}</div>}` — this MUST be conditional render (`&&`), NOT CSS display:none, per FILT-04 and Pitfall 4. Clear-all button: shadcn `<Button variant="ghost">הצג הכל</Button>` rendered only when `hasActiveFilters = selectedPhotographers.size > 0 || selectedPhases.size > 0`. Use logical Tailwind utilities `ps-`/`pe-`/`ms-`/`me-` exclusively; no `pl-`/`pr-`/`ml-`/`mr-`.

    3. **site/src/components/Gallery.jsx** — Use the canonical shape from 04-PATTERNS.md `site/src/components/Gallery.jsx` section. Imports PHASE_ORDER from `@/config`, GallerySection from `@/components/GallerySection`, EmptyState from `@/components/EmptyState`. Compute `hasAnyPhoto = PHASE_ORDER.some(phase => (filteredByPhase[phase] || []).length > 0)`; render `<EmptyState />` if false. Otherwise iterate `PHASE_ORDER.map(phase => { const sectionPhotos = filteredByPhase[phase] || []; if (sectionPhotos.length === 0) return null; return <GallerySection key={phase} phase={phase} photos={sectionPhotos} flatFilteredPhotos={flatFilteredPhotos} onPhotoClick={onPhotoClick} />; })`. The `return null` ensures empty sections are NOT in the DOM (D-05, Pitfall 4) — do NOT wrap in a hidden div.

    4. **site/src/components/GallerySection.jsx** — Use the canonical shape from 04-PATTERNS.md `site/src/components/GallerySection.jsx` section. Define `BREAKPOINT_COLS = { default: 5, 1280: 5, 1024: 4, 640: 3, 0: 2 }` at module scope. Section header: `<div className="sticky top-16 z-[5] bg-stone-50/90 backdrop-blur-sm py-3 mb-4">` with `<h2 className="text-[22px] font-semibold leading-tight text-stone-800">{PHASE_LABELS[phase]}<span className="ms-2 text-sm font-normal text-stone-500">({photos.length} תמונות)</span></h2>`. Below that, render `<Masonry breakpointCols={BREAKPOINT_COLS} className="masonry-grid" columnClassName="masonry-grid_column">{photos.map(photo => { const flatIndex = flatFilteredPhotos.indexOf(photo); return <PhotoCard key={photo.id} photo={photo} onClick={() => onPhotoClick(flatIndex)} />; })}</Masonry>`.

    5. **site/src/components/PhotoCard.jsx** — Use the canonical shape from 04-PATTERNS.md `site/src/components/PhotoCard.jsx` section. Container: `<div className="relative group cursor-pointer overflow-hidden rounded-sm mb-0" onClick={onClick}>`. Image: `<img src={photo.thumb_url} alt="" loading="lazy" className="w-full h-auto block transition-transform duration-300 group-hover:scale-[1.02]" />`.

       **Badge position — D-13 corner badge rationale (READ BEFORE WRITING JSX):**
       D-13 specifies "subtle badge at the bottom-right corner on hover." Because the document is RTL (`dir="rtl"` on `<html>`), Tailwind logical utilities flip:
       - `end-0` resolves to `right: 0` in LTR but `left: 0` in RTL → visual *bottom-left*. WRONG for D-13.
       - `start-0` resolves to `left: 0` in LTR but `right: 0` in RTL → visual *bottom-right*. CORRECT for D-13.
       Per UI-SPEC §5.4 the alternative is a full-width gradient overlay. We are taking the corner-badge approach (D-13 literal) — full-width gradient is NOT adopted. Use `bottom-0 start-0` so the visual position is the RTL bottom-right corner. Do NOT use `end-0`, `right-0`, or `left-0` on this element.

       Hover badge JSX: `<div className="absolute bottom-0 start-0 m-1.5 px-2 py-1 rounded bg-black/50 text-white text-[13px] leading-snug opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none">{PHOTOGRAPHER_NAMES[photo.photographer] ?? photo.photographer}</div>`.

    6. **site/src/components/Lightbox.jsx** — Use the canonical shape from 04-PATTERNS.md `site/src/components/Lightbox.jsx` section. Imports `Lightbox` from 'yet-another-react-lightbox' (the default export — rename via alias if needed to avoid collision with our wrapper's name) and the stylesheet `'yet-another-react-lightbox/styles.css'`. Default-export a function named `LightboxWrapper` accepting `{ open, index, slides, onClose }`. Map slides: `const yarlSlides = slides.map(photo => ({ src: photo.r2_url, alt: photo.id }))`. Render `<Lightbox open={open} close={onClose} slides={yarlSlides} index={index} portal={{ container: { dir: 'rtl' } }} />`.

    7. **site/src/components/LoadingSkeleton.jsx** — Use the canonical shape from 04-PATTERNS.md `site/src/components/LoadingSkeleton.jsx` section. Full-screen stone-50 bg, skeleton filter bar (`h-16 bg-stone-100 rounded mb-8 animate-pulse`), then a 2/3/4-col responsive grid of 12 skeleton cards with varied aspect ratios.

    8. **site/src/components/ErrorState.jsx** — Use the canonical shape from 04-PATTERNS.md `site/src/components/ErrorState.jsx` section. Centered card. Hebrew copy from UI-SPEC §5.7 and §6: heading 'לא ניתן לטעון את התמונות', optional `error?.message` in stone-400, retry `<Button onClick={() => window.location.reload()}>רענן</Button>`.

    9. **site/src/components/EmptyState.jsx** — Use the canonical shape from 04-PATTERNS.md `site/src/components/EmptyState.jsx` section. Centered Hebrew message 'אין תמונות התואמות לסינון הנוכחי'.

    After implementation, verify the dev server runs and the build succeeds: `cd site && npm run build` should exit 0. Do NOT manually open a browser in this task — Plan 04 handles visual verification.
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -20 && for f in src/App.jsx src/components/Filters.jsx src/components/Gallery.jsx src/components/GallerySection.jsx src/components/PhotoCard.jsx src/components/Lightbox.jsx src/components/LoadingSkeleton.jsx src/components/ErrorState.jsx src/components/EmptyState.jsx; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && grep -l "people.length > 0" src/components/Filters.jsx && grep -l "loading=\"lazy\"" src/components/PhotoCard.jsx && grep -l "react-masonry-css" src/components/GallerySection.jsx && grep -l "yet-another-react-lightbox" src/components/Lightbox.jsx && grep -l "PHASE_ORDER" src/components/Gallery.jsx && grep -q "start-0" src/components/PhotoCard.jsx && ! grep -nE "\\bend-0\\b|\\bright-0\\b|\\bleft-0\\b" src/components/PhotoCard.jsx && ! grep -nE "\\bpl-[0-9]|\\bpr-[0-9]|\\bml-[0-9]|\\bmr-[0-9]" src/components/*.jsx</automated>
  </verify>
  <acceptance_criteria>
    - All 9 component files exist at the paths listed in <files> above
    - site/src/App.jsx imports `usePhotos` from `@/hooks/usePhotos` AND `useFilters` from `@/hooks/useFilters`
    - site/src/App.jsx renders LoadingSkeleton when `loading`, ErrorState when `error`, and Filters+main+LightboxWrapper otherwise (verify via grep: file contains `if (loading)`, `if (error)`, and `<LightboxWrapper`)
    - site/src/components/Filters.jsx contains the literal `people.length > 0` and `&&` for the face filter conditional render (NOT `display: 'none'` or `hidden`)
    - site/src/components/Filters.jsx contains the literal Hebrew string `הצג הכל` for the clear-all button
    - site/src/components/Filters.jsx imports `PHOTOGRAPHER_NAMES`, `PHASE_LABELS`, `PHASE_ORDER` from `@/config`
    - site/src/components/Filters.jsx imports `Button` from `@/components/ui/button`
    - site/src/components/Gallery.jsx iterates `PHASE_ORDER` (verify via grep `PHASE_ORDER.map` or `PHASE_ORDER.some`) AND contains `return null` for the empty-section case
    - site/src/components/GallerySection.jsx imports `Masonry` from `react-masonry-css` AND uses className `masonry-grid` AND columnClassName `masonry-grid_column`
    - site/src/components/GallerySection.jsx contains `BREAKPOINT_COLS` with at least 5 breakpoints AND uses `PHASE_LABELS[phase]` for the header text
    - site/src/components/PhotoCard.jsx contains the literal `loading="lazy"` on its img element
    - site/src/components/PhotoCard.jsx positions the badge with `bottom-0 start-0` (logical; resolves to visual bottom-right in RTL per D-13) AND does NOT contain `end-0`, `right-0`, or `left-0` on the badge
    - site/src/components/PhotoCard.jsx uses `PHOTOGRAPHER_NAMES[photo.photographer]` (not photo.photographer directly — D-14)
    - site/src/components/Lightbox.jsx imports `Lightbox` from `yet-another-react-lightbox` AND imports `yet-another-react-lightbox/styles.css`
    - site/src/components/Lightbox.jsx maps `slides` to `{ src: photo.r2_url, ... }` AND passes `portal={{ container: { dir: 'rtl' } }}`
    - site/src/components/Lightbox.jsx default-exports a function (named `LightboxWrapper` or `Lightbox`; App imports it as default)
    - site/src/components/ErrorState.jsx imports `Button` from `@/components/ui/button` AND contains literal Hebrew strings `לא ניתן לטעון את התמונות` and `רענן`
    - site/src/components/EmptyState.jsx contains the literal Hebrew string `אין תמונות התואמות לסינון הנוכחי`
    - site/src/components/LoadingSkeleton.jsx contains the class `animate-pulse`
    - `cd site && npm run build` exits 0 with no errors
    - `grep -nE "\bpl-[0-9]|\bpr-[0-9]|\bml-[0-9]|\bmr-[0-9]" site/src/components/*.jsx` returns ZERO matches (all directional utilities are logical)
  </acceptance_criteria>
  <done>All 9 components implemented per UI-SPEC and PATTERNS; App.jsx wires hooks; `npm run build` succeeds; no physical-directional Tailwind utilities in component files; PhotoCard badge sits at the visual RTL bottom-right corner per D-13; ready for component tests in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Component tests — Filters (FILT-04) + Gallery (GALL-02) + Lightbox (GALL-03 + GALL-04)</name>
  <files>site/tests/Filters.test.jsx, site/tests/Gallery.test.jsx, site/tests/Lightbox.test.jsx</files>
  <read_first>
    - site/src/components/Filters.jsx (the SUT — read after Task 1 implements it)
    - site/src/components/Gallery.jsx (the SUT)
    - site/src/components/GallerySection.jsx (rendered by Gallery — informs how to query the DOM in Gallery tests)
    - site/src/components/PhotoCard.jsx (rendered by GallerySection — informs onClick wiring for Lightbox tests)
    - site/src/components/Lightbox.jsx (the SUT — informs how to assert on slides prop)
    - site/src/config.js (PHASE_LABELS, PHASE_ORDER for assertions)
    - .planning/phases/04-react-site/04-CONTEXT.md (D-05, D-09, D-12 — semantics under test)
    - .planning/phases/04-react-site/04-RESEARCH.md §Validation Architecture (test file targets)
  </read_first>
  <behavior>
    Filters.test.jsx (3+ tests):
    - Test F1 (FILT-04 absence): rendering Filters with `people={[]}` does NOT include a `[data-testid="face-filter"]` element in the DOM (use `queryByTestId` and assert null — NOT getByTestId)
    - Test F2 (FILT-04 presence): rendering Filters with `people={[{id:'p1',name:'Test'}]}` DOES include `[data-testid="face-filter"]` in the DOM
    - Test F3 (chip render): all 3 photographer chips render with their PHOTOGRAPHER_NAMES values, all 5 phase chips render with their PHASE_LABELS values
    - Test F4 (clear-all visibility): when both selection Sets are empty, the 'הצג הכל' button is NOT in the DOM; when selectedPhotographers has at least one entry, 'הצג הכל' IS in the DOM (clicking it invokes onClearAll)

    Gallery.test.jsx (3+ tests):
    - Test G1 (GALL-02 PHASE_ORDER rendering): given filteredByPhase with all 5 phases non-empty, the rendered sections appear in PHASE_ORDER (use `getAllByRole('heading', { level: 2 })` and assert text content matches `PHASE_ORDER.map(p => PHASE_LABELS[p])`)
    - Test G2 (D-05 empty section absence): given filteredByPhase where 2 phases have empty arrays and 3 have photos, exactly 3 h2 headings appear in the DOM (not 5 with hidden styling) — verify the empty-phase Hebrew labels are NOT in the document via `queryByText`
    - Test G3 (all-empty → EmptyState): given filteredByPhase where every phase is empty, the EmptyState Hebrew message 'אין תמונות התואמות לסינון הנוכחי' is in the document

    Lightbox.test.jsx (2+ tests):
    - Test L1 (GALL-03 open on click → GALL-04 slides=filtered): render a parent test harness that uses useFilters + LightboxWrapper; simulate a click on a PhotoCard; assert the Lightbox is open AND the `slides` prop passed to yarl equals the flatFilteredPhotos.map(p => ({src: p.r2_url, alt: p.id})) — NOT all 1300 photos. Easiest pattern: spy/mock the yarl `Lightbox` default export with `vi.mock('yet-another-react-lightbox', () => ({ default: vi.fn(() => null) }))` then assert on the mock's call args (`expect(yarlMock).toHaveBeenCalledWith(expect.objectContaining({ slides, open: true, index: clickedIndex }), expect.anything())`). Also `vi.mock('yet-another-react-lightbox/styles.css', () => ({}))` to silence the CSS import.
    - Test L2 (GALL-04 filtered subset, not all): build a fixture where 3 of 5 photos match the active filter; click the 2nd matching photo (flatIndex=1); assert the yarl mock was called with `slides.length === 3` AND `index === 1` AND each slide.src corresponds to a filtered photo's r2_url (NOT any of the 2 unfiltered photos)
  </behavior>
  <action>
    1. Create `site/tests/Filters.test.jsx` with Tests F1–F4. Import React, render, screen, fireEvent from '@testing-library/react'. Import Filters from `@/components/Filters`. Build minimal props per test. Use `data-testid="face-filter"` on the conditional element — if Plan 03 Task 1 didn't include that testid, add it during Task 2 to enable assertion. (The acceptance for Task 1 specified the conditional render but the testid is a Task 2 addition for test stability.) Wrap state-changing assertions in act if needed; most chip-rendering tests are pure render assertions.

    2. Create `site/tests/Gallery.test.jsx` with Tests G1–G3. Import Gallery from `@/components/Gallery`. Build `filteredByPhase` and `flatFilteredPhotos` fixtures by hand (use the same per-phase shape as useFilters produces). Provide a noop `onPhotoClick`. Note: Gallery renders GallerySection which renders Masonry which renders PhotoCard — the full tree renders during test, so include valid photo objects with `id`, `thumb_url`, `r2_url`, `photographer`, `cluster`, `sort_key` fields.

    3. Create `site/tests/Lightbox.test.jsx` with Tests L1–L2. Use Vitest module mocking at the top of the file:
       ```
       import { vi } from 'vitest';
       vi.mock('yet-another-react-lightbox/styles.css', () => ({}));
       const yarlMock = vi.fn(() => null);
       vi.mock('yet-another-react-lightbox', () => ({ default: yarlMock }));
       ```
       Then import LightboxWrapper from `@/components/Lightbox` AFTER the mocks. Build a test harness component that mounts useFilters with a static photos array, mounts LightboxWrapper, exposes a click handler. Use `fireEvent.click` on a rendered PhotoCard. Assert `yarlMock` was called with the expected props using `expect(yarlMock).toHaveBeenLastCalledWith(expect.objectContaining({ open: true, index: <expected>, slides: expect.arrayContaining([...]) }), expect.anything())`.

    4. For Test L2 (filtered subset): construct 5 photos with mixed photographers; activate a filter that excludes 2; click the 2nd remaining photo; assert `yarlMock.mock.calls[lastCall][0].slides.length === 3` and each slide.src is one of the 3 matching photos' r2_urls.

    5. If Task 1's Filters.jsx did not include `data-testid="face-filter"` on the conditional wrapper, edit Filters.jsx to add it in this task. This is the only edit to a component file in Task 2.

    6. Run `cd site && npm test -- --run` and verify the full test suite passes — Plan 01 + Plan 02 + Plan 03 tests all green.
  </action>
  <verify>
    <automated>cd site && npm test -- --run --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - site/tests/Filters.test.jsx contains at least 4 `it(` blocks; uses `queryByTestId` (not `getByTestId`) for the FILT-04 absence assertion
    - site/tests/Gallery.test.jsx contains at least 3 `it(` blocks; asserts PHASE_ORDER ordering of rendered h2 headings AND asserts EmptyState renders when all sections are empty
    - site/tests/Lightbox.test.jsx contains at least 2 `it(` blocks AND uses `vi.mock('yet-another-react-lightbox', ...)` to intercept the yarl default export AND asserts on `slides` prop equaling the filtered subset (NOT all photos)
    - site/src/components/Filters.jsx contains `data-testid="face-filter"` on the conditionally rendered element (added in this task if not present after Task 1)
    - `cd site && npm test -- --run` exits 0 with ALL tests passing (Plan 01 + Plan 02 + Plan 03 combined)
    - `grep -vc '^#' site/tests/Filters.test.jsx | xargs -I{} sh -c 'echo {}'` is a placeholder; the real gate is: `grep -c "^\s*it(" site/tests/Filters.test.jsx` returns ≥ 4
    - `grep -c "^\s*it(" site/tests/Gallery.test.jsx` returns ≥ 3
    - `grep -c "^\s*it(" site/tests/Lightbox.test.jsx` returns ≥ 2
    - `grep -q "queryByTestId.*face-filter" site/tests/Filters.test.jsx` (asserts absence test exists)
    - `grep -q "vi.mock.*yet-another-react-lightbox" site/tests/Lightbox.test.jsx` (asserts yarl mocking pattern)
  </acceptance_criteria>
  <done>FILT-04 (absence + presence), GALL-02 (PHASE_ORDER ordering + empty-section absence), GALL-03 (open on click), GALL-04 (slides=filtered subset) all covered with automated tests; full Vitest suite green across all three plans.</done>
</task>

</tasks>

<verification>
- `cd site && npm run build` exits 0
- `cd site && npm test -- --run` exits 0 with all tests passing (Plans 01 + 02 + 03)
- `! grep -nE "\bpl-[0-9]|\bpr-[0-9]|\bml-[0-9]|\bmr-[0-9]" site/src/components/*.jsx` (no physical-directional utilities)
- `grep -l 'people.length > 0' site/src/components/Filters.jsx` (FILT-04 conditional)
- `grep -l 'loading="lazy"' site/src/components/PhotoCard.jsx` (perf invariant)
- `grep -l 'PHASE_ORDER' site/src/components/Gallery.jsx` (D-04 iteration source)
- `grep -q 'start-0' site/src/components/PhotoCard.jsx` (D-13 badge at visual RTL bottom-right corner)
</verification>

<success_criteria>
Running `cd site && npm run dev` opens a Hebrew-RTL gallery showing the synthetic 15-photo fixture from Plan 01. Filter chips work — selecting a photographer narrows the visible photos in every section; selecting a phase collapses other sections; combining both filters demonstrates AND-across logic. Clicking any thumbnail opens a lightbox that previews only the currently filtered set. The 'הצג הכל' button resets the view. The face filter is absent from the DOM (verifiable via devtools). All visible UI text is Hebrew. The Vitest suite covers all measurable invariants. Plan 04 can perform visual verification against this build.
</success_criteria>

<output>
Create `.planning/phases/04-react-site/04-03-SUMMARY.md` when done.
</output>
