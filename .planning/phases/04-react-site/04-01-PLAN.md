---
phase: 04-react-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/package.json
  - site/package-lock.json
  - site/index.html
  - site/vite.config.js
  - site/jsconfig.json
  - site/components.json
  - site/src/index.css
  - site/src/main.jsx
  - site/src/App.jsx
  - site/src/config.js
  - site/src/components/ui/button.jsx
  - site/public/metadata.json
  - site/tests/setup.js
  - site/tests/config.test.js
  - site/tests/smoke.test.jsx
autonomous: true
requirements:
  - I18N-02
  - I18N-03
  - DSGN-01
tags:
  - vite
  - tailwind
  - shadcn
  - rtl
  - hebrew
  - react
must_haves:
  truths:
    - "site/ directory exists with a working Vite + React 19 + Tailwind v4 + shadcn baseline"
    - "Running `npm run dev` inside site/ serves a page with dir=rtl and lang=he on <html>"
    - "Running `npm run build` inside site/ produces a clean dist/ with no errors"
    - "Running `npm test -- --run` inside site/ executes Vitest in jsdom and exits 0"
    - "site/src/config.js exports PHASE_LABELS, PHASE_ORDER, PHOTOGRAPHER_NAMES, METADATA_URL with the canonical shape"
    - "site/public/metadata.json synthetic fixture is fetchable at /metadata.json in dev"
  artifacts:
    - path: "site/package.json"
      provides: "Vite + React 19 + Tailwind v4 + shadcn + yarl + masonry + Heebo + Vitest deps"
      contains: "react"
    - path: "site/index.html"
      provides: "HTML shell with lang=he dir=rtl on <html>, Hebrew title 'האלבום שלנו'"
      contains: "dir=\"rtl\""
    - path: "site/vite.config.js"
      provides: "Vite config with @vitejs/plugin-react, @tailwindcss/vite, @ alias to ./src, vitest jsdom config"
      contains: "@tailwindcss/vite"
    - path: "site/jsconfig.json"
      provides: "Path alias @/* → ./src/* for shadcn import resolution in a JS project"
      contains: "@/*"
    - path: "site/src/index.css"
      provides: "Tailwind v4 single-import + Heebo font import + @theme block + react-masonry-css CSS"
      contains: "@import \"tailwindcss\""
    - path: "site/src/config.js"
      provides: "PHASE_LABELS, PHASE_ORDER, PHOTOGRAPHER_NAMES, METADATA_URL canonical exports"
      contains: "PHASE_ORDER"
    - path: "site/src/main.jsx"
      provides: "React 19 createRoot mount of <App /> inside <StrictMode>"
      contains: "createRoot"
    - path: "site/src/App.jsx"
      provides: "Stub root component (full wiring in Plan 03); renders 'האלבום שלנו' so smoke test passes"
      contains: "export default"
    - path: "site/src/components/ui/button.jsx"
      provides: "shadcn-generated Button component (will be consumed by ErrorState and Filters in Plan 03)"
      contains: "export"
    - path: "site/public/metadata.json"
      provides: "Synthetic dev fixture with photos across all 5 phases and all 3 photographer labels, empty people[]"
      contains: "abir_sultan"
    - path: "site/tests/setup.js"
      provides: "Shared @testing-library/jest-dom setup imported by vitest"
      contains: "@testing-library/jest-dom"
    - path: "site/tests/config.test.js"
      provides: "Wave 0 RED test for I18N-03: PHASE_LABELS/PHASE_ORDER/PHOTOGRAPHER_NAMES match the canonical Hebrew map"
      contains: "PHASE_LABELS"
    - path: "site/tests/smoke.test.jsx"
      provides: "Wave 0 smoke test: App renders without throwing; <html> has dir=rtl"
      contains: "render"
  key_links:
    - from: "site/vite.config.js"
      to: "site/src/index.css"
      via: "@tailwindcss/vite plugin transforms @import \"tailwindcss\""
      pattern: "tailwindcss\\(\\)"
    - from: "site/jsconfig.json"
      to: "site/src/*"
      via: "@/* path alias"
      pattern: "@/\\*"
    - from: "site/src/main.jsx"
      to: "site/src/App.jsx"
      via: "import App from '@/App'"
      pattern: "from\\s+['\"]@/App['\"]"
    - from: "site/src/App.jsx"
      to: "site/public/metadata.json"
      via: "fetch via Plan 02 usePhotos (METADATA_URL default '/metadata.json')"
      pattern: "/metadata\\.json"
---

<objective>
Scaffold the entire `site/` directory from scratch as a working Vite + React 19 + Tailwind CSS v4 + shadcn/ui baseline with Hebrew RTL HTML shell, install all runtime + dev dependencies (including Vitest + Testing Library), write `site/src/config.js` (the canonical I18N-03 phase-label and photographer-name map), generate a synthetic local-dev `metadata.json` fixture covering all phases/photographers, and stand up the Wave-0 test infrastructure with two RED test files so Plans 02 and 03 can write tests against a real test runner.

Purpose: Every downstream plan depends on this scaffold — there is no `site/` directory yet. Plan 02 (hooks) needs Vitest + jsdom + `@/config` imports. Plan 03 (components) needs shadcn `Button`, Tailwind v4 classes, the dev fixture, and a working `npm run dev`. Plan 04 (visual verify) needs `npm run build` and `npm run dev` to function.

Output: A scaffolded `site/` directory whose `npm install`, `npm run dev`, `npm run build`, and `npm test -- --run` commands all succeed. `site/src/config.js` is the single source of truth for Hebrew phase labels (I18N-03). RTL/lang=he is established at the HTML root (I18N-02 foundation). shadcn baseline (Button component generated) is in place (DSGN-01 foundation).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-react-site/04-CONTEXT.md
@.planning/phases/04-react-site/04-RESEARCH.md
@.planning/phases/04-react-site/04-PATTERNS.md
@.planning/phases/04-react-site/04-UI-SPEC.md
@.planning/phases/04-react-site/04-VALIDATION.md
@pipeline/config.yaml
@pipeline/output/metadata.json

<interfaces>
<!-- Canonical config.js shape — copy verbatim. Per CONTEXT.md D-14, D-15 and UI-SPEC §11. -->
<!-- Photographer keys verified against pipeline/output/metadata.json. Display names from pipeline/config.yaml lines 38-46. -->

PHASE_LABELS (object): { prep: 'הכנות', photoshooting: 'צילומים', dining: 'ארוחה', hupa: 'חופה', dancing: 'ריקודים' }
PHASE_ORDER (array): ['prep', 'photoshooting', 'dining', 'hupa', 'dancing']  // actual wedding timeline; do NOT alphabetize
PHOTOGRAPHER_NAMES (object): { abir_sultan: 'עביר סולטן', inbal_zeldin: 'ענבל זלדין', magnate_images: 'Magnate Images' }
METADATA_URL (string): import.meta.env.VITE_METADATA_URL || '/metadata.json'

metadata.json runtime schema (from pipeline/output/metadata.json):
{
  "photos": [
    { "id": str, "filename": str, "r2_url": str, "thumb_url": str,
      "photographer": "abir_sultan"|"inbal_zeldin"|"magnate_images",
      "timestamp": ISO8601 str, "cluster": "prep"|"photoshooting"|"dining"|"hupa"|"dancing",
      "cluster_confidence": float, "faces": [], "sort_key": int }
  ],
  "people": []
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vite + React 19 scaffold, Tailwind v4 + shadcn init, install all deps, RTL HTML shell</name>
  <files>site/package.json, site/package-lock.json, site/index.html, site/vite.config.js, site/jsconfig.json, site/components.json, site/src/index.css, site/src/main.jsx, site/src/App.jsx, site/src/components/ui/button.jsx</files>
  <read_first>
    - .planning/phases/04-react-site/04-RESEARCH.md (installation steps, Tailwind v4 + shadcn JS pitfalls, package versions)
    - .planning/phases/04-react-site/04-PATTERNS.md (site/index.html, site/vite.config.js, site/jsconfig.json, site/src/index.css, site/src/main.jsx canonical shapes)
    - .planning/phases/04-react-site/04-UI-SPEC.md sections 1, 3, 4, 7 (design system init, Heebo typography, color palette, RTL contract)
    - .planning/phases/04-react-site/04-CONTEXT.md (D-01 Tailwind+shadcn, D-02 RTL via dir=rtl)
  </read_first>
  <behavior>
    - Test 1 (smoke): `render(<App />)` does not throw; document body contains the Hebrew page title 'האלבום שלנו'
    - Test 2 (RTL): the rendered document/html element has dir="rtl" and lang="he"
    - Build: `cd site && npm run build` exits 0 producing site/dist/
  </behavior>
  <action>
    Scaffold the React site from scratch. Steps in order:

    1. From repo root, run: `npm create vite@latest site -- --template react` (accept any prompts). Then `cd site && npm install`. This creates `site/package.json`, `site/index.html`, `site/src/main.jsx`, `site/src/App.jsx`, and standard Vite files.

    2. Install Tailwind v4 + path-alias support + runtime deps + test deps (single npm install per group):
       - `npm install tailwindcss @tailwindcss/vite`
       - `npm install -D @types/node` (needed for `path` in vite.config.js + shadcn alias resolution per RESEARCH §Pitfall 2)
       - `npm install react-masonry-css yet-another-react-lightbox @fontsource/heebo tw-animate-css`
       - `npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom`

    3. Create `site/jsconfig.json` BEFORE running shadcn init (per RESEARCH §Pitfall 2). Use the canonical shape from 04-PATTERNS.md `site/jsconfig.json` section: `{ "compilerOptions": { "baseUrl": ".", "paths": { "@/*": ["./src/*"] } } }`.

    4. Replace `site/vite.config.js` with the canonical shape from 04-PATTERNS.md `site/vite.config.js` section. Plugins must be `react()` and `tailwindcss()`. Alias `@` → `./src`. ALSO add Vitest config: `test: { environment: 'jsdom', globals: true, setupFiles: ['./tests/setup.js'] }`.

    5. Replace `site/src/index.css` ENTIRELY with the canonical shape from 04-PATTERNS.md `site/src/index.css` section. This is: `@import "tailwindcss"` (single line — no v3 three-line pattern), then `@import "@fontsource/heebo/400.css"` and `/600.css`, then `@import "tw-animate-css"`, then a `@theme` block with `--color-accent: oklch(70% 0.06 65)`, `--color-accent-hover: oklch(63% 0.06 65)`, `--font-family-sans: 'Heebo', system-ui, sans-serif`, then the required `.masonry-grid` + `.masonry-grid_column` flex CSS with responsive gap breakpoints at 640px and 1024px.

    6. Replace `site/index.html` with the canonical shape from 04-PATTERNS.md `site/index.html` section. Must include `<html lang="he" dir="rtl">` and `<title>האלבום שלנו</title>`. The Vite-default script tag pointing to `/src/main.jsx` is preserved.

    7. Replace `site/src/main.jsx` with the canonical shape from 04-PATTERNS.md `site/src/main.jsx` section: imports `@/index.css` and `@/App`, mounts with `createRoot` inside `<StrictMode>`.

    8. Replace `site/src/App.jsx` with a MINIMAL stub that just renders the page title in an RTL-friendly container. This is a placeholder — Plan 03 replaces this file with the full wired root component. The stub must satisfy the smoke test by rendering 'האלבום שלנו' somewhere in the DOM. Use Tailwind classes for styling (e.g., `min-h-screen bg-stone-50 flex items-center justify-center text-stone-800 text-3xl font-semibold`).

    9. Run shadcn init: `npx shadcn@latest init`. Answer prompts to match: framework=Vite, language=JavaScript, base color=Stone, use CSS variables=yes. This generates `site/components.json` and updates `site/src/index.css` with shadcn CSS variables. Verify the `@import "tailwindcss"` line is still present after init; if shadcn rewrote the file, merge our `@theme` block, Heebo imports, `tw-animate-css` import, and `.masonry-grid` CSS back in.

    10. Generate the Button component: `npx shadcn@latest add button`. Creates `site/src/components/ui/button.jsx` (note: `.jsx` not `.tsx` because of JS project).

    11. Verify `cd site && npm run build` exits 0 producing `site/dist/`. Verify `cd site && npm run dev` starts without errors (kill after confirming startup).
  </action>
  <verify>
    <automated>cd site && npm run build 2>&1 | tail -20 && test -f site/dist/index.html && test -f site/src/components/ui/button.jsx && grep -q 'dir="rtl"' site/index.html && grep -q 'lang="he"' site/index.html && grep -q '@import "tailwindcss"' site/src/index.css && grep -q '@tailwindcss/vite' site/vite.config.js && grep -q '"@/\*"' site/jsconfig.json && grep -q 'masonry-grid' site/src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - site/package.json contains "react", "vite", "tailwindcss", "@tailwindcss/vite", "react-masonry-css", "yet-another-react-lightbox", "@fontsource/heebo", "tw-animate-css", "vitest", "@testing-library/react", "@testing-library/jest-dom", "jsdom" in dependencies or devDependencies
    - site/index.html contains the exact strings `lang="he"` AND `dir="rtl"` on the <html> tag, and `<title>האלבום שלנו</title>` in <head>
    - site/jsconfig.json contains `"@/*": ["./src/*"]` under compilerOptions.paths
    - site/vite.config.js imports `@tailwindcss/vite` and `@vitejs/plugin-react`, registers both in plugins array, sets alias `@` → `./src` via path.resolve, AND has a `test` block with `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./tests/setup.js']`
    - site/src/index.css contains exactly one `@import "tailwindcss"` line, `@import "@fontsource/heebo/400.css"`, `@import "@fontsource/heebo/600.css"`, `@import "tw-animate-css"`, a `@theme {` block defining `--color-accent`, `--color-accent-hover`, `--font-family-sans`, AND `.masonry-grid { display: flex` and `.masonry-grid_column { display: flex; flex-direction: column` rules
    - site/components.json exists (shadcn init produced it)
    - site/src/components/ui/button.jsx exists and exports a Button component
    - site/src/main.jsx imports from `@/index.css` and `@/App` and calls `createRoot(...).render(<StrictMode><App /></StrictMode>)`
    - site/src/App.jsx is a default-exported function component that renders the Hebrew string 'האלבום שלנו' somewhere in its returned JSX
    - `cd site && npm run build` exits 0 and produces site/dist/index.html
  </acceptance_criteria>
  <done>Vite scaffold complete, all deps installed, RTL+Hebrew HTML shell in place, Tailwind v4 + shadcn baseline working, Button component generated, `npm run build` passes, ready for hooks (Plan 02) and components (Plan 03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write site/src/config.js + synthetic metadata.json fixture + Wave-0 test infrastructure</name>
  <files>site/src/config.js, site/public/metadata.json, site/tests/setup.js, site/tests/config.test.js, site/tests/smoke.test.jsx</files>
  <read_first>
    - .planning/phases/04-react-site/04-PATTERNS.md (site/src/config.js canonical shape — lines describing PHASE_LABELS/PHASE_ORDER/PHOTOGRAPHER_NAMES/METADATA_URL)
    - .planning/phases/04-react-site/04-UI-SPEC.md §11 (file structure contract, config.js canonical shape) and §6 (copywriting contract)
    - .planning/phases/04-react-site/04-RESEARCH.md §Validation Architecture (Vitest setup, jsdom environment, Wave 0 gaps)
    - .planning/phases/04-react-site/04-CONTEXT.md (D-12 face filter gate with empty people[]; D-14 real names; D-15 config.js phase map)
    - pipeline/config.yaml lines 38-46 (photographer label → display_name mapping — must mirror exactly)
    - pipeline/output/metadata.json (verify schema field names: id, filename, r2_url, thumb_url, photographer, timestamp, cluster, cluster_confidence, faces, sort_key)
    - site/src/App.jsx (the stub from Task 1, so the smoke test renders it)
  </read_first>
  <behavior>
    - Test 1 (config.test.js, I18N-03): PHASE_LABELS has exactly 5 keys {prep, photoshooting, dining, hupa, dancing} mapping to {הכנות, צילומים, ארוחה, חופה, ריקודים}
    - Test 2 (config.test.js, I18N-03): PHASE_ORDER equals ['prep', 'photoshooting', 'dining', 'hupa', 'dancing'] in that exact order (wedding timeline, not alphabetical)
    - Test 3 (config.test.js): PHOTOGRAPHER_NAMES has keys {abir_sultan, inbal_zeldin, magnate_images} mapping to {עביר סולטן, ענבל זלדין, Magnate Images}
    - Test 4 (config.test.js): METADATA_URL defaults to '/metadata.json' when VITE_METADATA_URL is unset
    - Test 5 (smoke.test.jsx): rendering <App /> does not throw and the rendered output contains the Hebrew string 'האלבום שלנו'
  </behavior>
  <action>
    1. Create `site/src/config.js` with the canonical shape from 04-PATTERNS.md `site/src/config.js` section. Copy verbatim — DO NOT deviate. Four named exports: `PHASE_LABELS` (Hebrew map for 5 phases), `PHASE_ORDER` (array in wedding timeline order: prep, photoshooting, dining, hupa, dancing — NOT alphabetical), `PHOTOGRAPHER_NAMES` (three keys matching metadata.json `photographer` field: `abir_sultan`, `inbal_zeldin`, `magnate_images`), and `METADATA_URL` (defaults to '/metadata.json' via `import.meta.env.VITE_METADATA_URL || '/metadata.json'`). Add a top-of-file comment: `// Canonical map per CONTEXT.md D-14, D-15 and UI-SPEC §11. Photographer keys verified against pipeline/output/metadata.json.`

    2. Create `site/public/metadata.json` — a synthetic dev fixture. Generate 15 entries (3 per phase × 5 phases = 15 photos). Each entry must include all fields from the runtime schema:
       - `id`: e.g., `"fixture_001"` through `"fixture_015"`
       - `filename`: e.g., `"fixture_001.jpg"`
       - `r2_url`: use a real public placeholder so the fixture is visually browsable in dev — `"https://picsum.photos/seed/fixture_001/1600/1200"` (vary the seed per photo and rotate orientations 1200x1600 vs 1600x1200 vs 1600x1600 so masonry has variable heights)
       - `thumb_url`: smaller version of the same picsum URL — `"https://picsum.photos/seed/fixture_001/400/300"` (matching aspect ratio per photo)
       - `photographer`: rotate through `abir_sultan`, `inbal_zeldin`, `magnate_images` (5 photos per photographer total)
       - `timestamp`: realistic ISO8601 strings within each phase's wedding-day time window per pipeline/config.yaml (e.g., prep photos 08:30–13:30, photoshooting 14:15–16:00, dining 16:30–17:45, hupa 18:05–18:35, dancing 19:00–22:00) all on "2025-06-14"
       - `cluster`: one of the 5 phase keys; distribute 3 photos per cluster
       - `cluster_confidence`: a float between 0.75 and 0.99
       - `faces`: empty array `[]` for every photo (FACE-XX is v2)
       - `sort_key`: integer 1..15, ascending by timestamp so display order is chronological
       Top-level: `{ "photos": [...15 entries...], "people": [] }`. `people` MUST be empty so FILT-04 face-filter-gate logic can be exercised by Plan 03 tests.

    3. Create `site/tests/setup.js` with a single import: `import '@testing-library/jest-dom'`. This activates jest-dom matchers (toBeInTheDocument, toHaveAttribute, etc.) for all test files.

    4. Create `site/tests/config.test.js` — Wave-0 RED test for I18N-03. Use Vitest `describe`/`it`/`expect`. Import all four exports from `@/config`. Assertions per the Behavior block above (5 tests). For Test 4 (METADATA_URL), since the env var is unset in test env, expect `METADATA_URL === '/metadata.json'`.

    5. Create `site/tests/smoke.test.jsx` — Wave-0 smoke test. Use `@testing-library/react` `render` and `screen`. Import App from `@/App`. Test renders `<App />` and asserts `screen.getByText(/האלבום שלנו/)` is in the document.

    6. Add a `test` script to `site/package.json` if not present: `"test": "vitest"` (Vite scaffold may or may not include it).

    7. Run `cd site && npm test -- --run` and verify all tests pass (exit 0). The smoke test passes because Task 1's App.jsx stub renders the Hebrew title. The config tests pass because config.js exports the canonical shape.
  </action>
  <verify>
    <automated>cd site && npm test -- --run --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - site/src/config.js exports `PHASE_LABELS` matching `{prep: 'הכנות', photoshooting: 'צילומים', dining: 'ארוחה', hupa: 'חופה', dancing: 'ריקודים'}` (5 keys, exact Hebrew strings)
    - site/src/config.js exports `PHASE_ORDER` as the array `['prep', 'photoshooting', 'dining', 'hupa', 'dancing']` in that exact order
    - site/src/config.js exports `PHOTOGRAPHER_NAMES` with keys `abir_sultan`, `inbal_zeldin`, `magnate_images` mapping to `'עביר סולטן'`, `'ענבל זלדין'`, `'Magnate Images'` respectively
    - site/src/config.js exports `METADATA_URL` defined as `import.meta.env.VITE_METADATA_URL || '/metadata.json'`
    - site/public/metadata.json parses as JSON with top-level `photos` array of length 15 AND top-level `people` array of length 0
    - site/public/metadata.json: every photo has all 10 schema fields (id, filename, r2_url, thumb_url, photographer, timestamp, cluster, cluster_confidence, faces, sort_key)
    - site/public/metadata.json: every `cluster` value is one of {prep, photoshooting, dining, hupa, dancing} AND every `photographer` value is one of {abir_sultan, inbal_zeldin, magnate_images}
    - site/public/metadata.json: each of the 5 clusters has at least 1 photo AND each of the 3 photographers has at least 1 photo
    - site/tests/setup.js imports `@testing-library/jest-dom`
    - site/tests/config.test.js asserts the canonical shape of all four config.js exports (5 assertions minimum)
    - site/tests/smoke.test.jsx renders App and asserts the Hebrew page title is in the document
    - `cd site && npm test -- --run` exits 0 with all tests passing
    - `python3 -c "import json; d=json.load(open('site/public/metadata.json')); assert len(d['photos'])==15; assert d['people']==[]; assert {p['cluster'] for p in d['photos']}=={'prep','photoshooting','dining','hupa','dancing'}; assert {p['photographer'] for p in d['photos']}=={'abir_sultan','inbal_zeldin','magnate_images'}"` exits 0
  </acceptance_criteria>
  <done>config.js is the single source of truth for I18N-03; synthetic metadata.json fixture covers all 5 phases × 3 photographers; Vitest infrastructure is live and green; Plans 02 and 03 can now write tests against `@/config` imports and the fixture.</done>
</task>

</tasks>

<verification>
- `cd site && npm run build` exits 0
- `cd site && npm test -- --run` exits 0 with at least 6 passing tests across config.test.js and smoke.test.jsx
- `grep -q 'dir="rtl"' site/index.html` AND `grep -q 'lang="he"' site/index.html`
- `cat site/public/metadata.json | python3 -m json.tool` parses successfully
- `test -f site/src/components/ui/button.jsx` (shadcn baseline)
- `grep -q "PHASE_ORDER" site/src/config.js` AND `grep -q "abir_sultan" site/src/config.js`
</verification>

<success_criteria>
A subsequent agent can `cd site && npm run dev` and see a Hebrew-titled RTL page load in the browser. A subsequent agent can `cd site && npm test -- --run` and see the 6+ Wave-0 tests pass. `@/config` and `@/components/ui/button` resolve correctly via the path alias. The dev fixture at `/metadata.json` is fetchable and contains 15 photos spanning all 5 phases and 3 photographers.
</success_criteria>

<output>
Create `.planning/phases/04-react-site/04-01-SUMMARY.md` when done.
</output>
