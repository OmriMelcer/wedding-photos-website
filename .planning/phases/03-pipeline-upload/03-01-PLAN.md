---
phase: 03-pipeline-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/config.yaml
  - pyproject.toml
autonomous: true
requirements:
  - UPLD-03

must_haves:
  truths:
    - "pipeline/config.yaml has an r2: block with bucket, endpoint, and r2_public_url keys"
    - "boto3 is declared in pyproject.toml dependencies"
    - "uv sync installs boto3 without error"
  artifacts:
    - path: "pipeline/config.yaml"
      provides: "R2 non-secret config (bucket, endpoint, r2_public_url)"
      contains: "r2:"
    - path: "pyproject.toml"
      provides: "boto3 dependency declaration"
      contains: "boto3"
  key_links:
    - from: "pipeline/upload.py"
      to: "pipeline/config.yaml"
      via: "yaml.safe_load reads config['r2']['bucket'], config['r2']['endpoint'], config['r2']['r2_public_url']"
      pattern: "r2_public_url"
---

<objective>
Add the `r2:` configuration block to `pipeline/config.yaml` and declare `boto3` as a dependency in `pyproject.toml`.

Purpose: `pipeline/upload.py` (Plan 03-02) reads non-secret R2 config — bucket name, S3-compatible endpoint URL, and public URL base — from `config.yaml`. Credentials stay in environment variables, not config. `boto3` provides the S3-compatible client for Cloudflare R2 uploads.

Output: Updated `config.yaml` with `r2:` block, updated `pyproject.toml` with `boto3>=1.35.0`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add r2: block to pipeline/config.yaml</name>
  <files>pipeline/config.yaml</files>

  <read_first>
    - pipeline/config.yaml — read the full file before editing; append after the existing `pipeline:` section
    - pipeline/cluster.py — see how config keys are accessed: `config["pipeline"]["events"]["time_windows"]`; follow the same top-level key style
    - .planning/phases/03-pipeline-upload/03-CONTEXT.md — D-01 and D-02: r2_public_url is the pub-xxx.r2.dev URL; credentials come from env vars, not this file
  </read_first>

  <action>
    Append a new top-level `r2:` block at the end of pipeline/config.yaml. The block must contain exactly these three keys:

    - `bucket`: the R2 bucket name (use placeholder string `"your-bucket-name"` — operator fills in real name before running)
    - `endpoint`: the S3-compatible endpoint URL for the bucket (use placeholder string `"https://ACCOUNT_ID.r2.cloudflarestorage.com"` — operator fills in real account ID)
    - `r2_public_url`: the public `pub-xxx.r2.dev` base URL (use placeholder string `"https://pub-REPLACE_ME.r2.dev"` — operator fills in after enabling public access on the bucket)

    Add a brief comment above the `r2:` block explaining that secrets (access key ID and secret access key) are passed via environment variables `R2_ACCESS_KEY_ID` and `R2_SECRET_ACCESS_KEY`, not stored here. This matches the existing comment in the file header:
    "Update R2 credentials in environment variables (see upload.py)."

    Do not modify any existing keys. Do not remove any existing sections.
  </action>

  <verify>
    <automated>grep -c "r2_public_url" /Users/omrimelcer/dev/wedding-photos-website/pipeline/config.yaml</automated>
    <automated>python3 -c "import yaml; c = yaml.safe_load(open('pipeline/config.yaml')); assert set(c['r2']) == {'bucket','endpoint','r2_public_url'}, c['r2']"</automated>
  </verify>

  <acceptance_criteria>
    - pipeline/config.yaml contains the key `r2:` at top level (not nested under `pipeline:`)
    - `config['r2']['bucket']` is a non-empty string
    - `config['r2']['endpoint']` is a non-empty string starting with `https://`
    - `config['r2']['r2_public_url']` is a non-empty string starting with `https://`
    - Existing `sources:`, `photographers:`, and `pipeline:` sections are unchanged
    - File parses with `yaml.safe_load` without error
    - A comment near the `r2:` block documents that `R2_ACCESS_KEY_ID` and `R2_SECRET_ACCESS_KEY` are env-var secrets
  </acceptance_criteria>

  <done>config.yaml has a parseable r2: block with bucket, endpoint, and r2_public_url; no existing keys removed; credential comment present</done>
</task>

<task type="auto">
  <name>Task 2: Add boto3 to pyproject.toml and sync</name>
  <files>pyproject.toml</files>

  <read_first>
    - pyproject.toml — read the current `dependencies` list before editing; boto3 is not yet listed
  </read_first>

  <action>
    Add `"boto3>=1.35.0"` to the `dependencies` list in pyproject.toml, maintaining alphabetical order (boto3 sorts before beautifulsoup4 alphabetically, so place it first, or follow the existing order which starts with beautifulsoup4 — in that case place boto3 before beautifulsoup4 since 'b-o' < 'b-e' is false; actually beautifulsoup4 comes before boto3 alphabetically so insert boto3 after beautifulsoup4 and before gallery-dl).

    After editing pyproject.toml, run `uv sync` from the project root to install boto3 into the virtual environment. Confirm the install succeeds with exit code 0.

    Do not add boto3 to `[dependency-groups]` dev section — it is a production runtime dependency of the upload pipeline, not a dev tool.
  </action>

  <verify>
    <automated>grep "boto3" /Users/omrimelcer/dev/wedding-photos-website/pyproject.toml</automated>
    <automated>cd /Users/omrimelcer/dev/wedding-photos-website && uv run python -c "import boto3; print(boto3.__version__)"</automated>
  </verify>

  <acceptance_criteria>
    - pyproject.toml `dependencies` list contains `"boto3>=1.35.0"` (or higher minimum version)
    - boto3 is in the `[project]` `dependencies` array, not in `[dependency-groups]`
    - `uv run python -c "import boto3"` exits 0 and prints a version string
    - No other dependencies are removed or changed
  </acceptance_criteria>

  <done>boto3 declared in pyproject.toml and importable via uv run</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → upload.py | Non-secret config values read from disk; secrets come from env vars |
| env vars → boto3 client | R2_ACCESS_KEY_ID and R2_SECRET_ACCESS_KEY are operator-supplied; not stored in any file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | pipeline/config.yaml | accept | Contains only non-secret config (bucket name, public endpoint). Secrets come from env vars. Committing config.yaml to git is intentional and safe. |
| T-03-02 | Tampering | pyproject.toml | mitigate | boto3 is a well-known AWS SDK package. Pinned with >= lower bound. Verify: `uv run python -c "import boto3"` after install. |
| T-03-SC | Tampering | pyproject.toml boto3 install | mitigate | boto3 is the canonical AWS SDK (pypi.org/project/boto3); published by Amazon. No slopcheck concern. |
</threat_model>

<verification>
1. `python3 -c "import yaml; c = yaml.safe_load(open('pipeline/config.yaml')); assert 'r2' in c"` exits 0
2. `python3 -c "import yaml; c = yaml.safe_load(open('pipeline/config.yaml')); assert set(c['r2']) == {'bucket','endpoint','r2_public_url'}"` exits 0
3. `grep boto3 pyproject.toml` shows `boto3>=1.35.0`
4. `uv run python -c "import boto3"` exits 0
5. Existing `pipeline:` section still intact: `python3 -c "import yaml; c = yaml.safe_load(open('pipeline/config.yaml')); assert 'confidence_threshold' in c['pipeline']"` exits 0
</verification>

<success_criteria>
- config.yaml has a parseable `r2:` block with three keys: bucket, endpoint, r2_public_url
- All three values are placeholder strings the operator will fill in before running upload.py
- A comment documents that credentials come from R2_ACCESS_KEY_ID and R2_SECRET_ACCESS_KEY env vars
- boto3 is importable in the project virtual environment via `uv run python -c "import boto3"`
- No existing config.yaml sections or pyproject.toml dependencies were removed
</success_criteria>

<output>
Create `.planning/phases/03-pipeline-upload/03-01-SUMMARY.md` when done
</output>
