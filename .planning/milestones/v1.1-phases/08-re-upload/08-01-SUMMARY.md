---
plan: 08-01
phase: 08-re-upload
status: complete
completed_at: 2026-05-18
---

# Plan 08-01 Summary: Re-run resize.py + upload.py, verify live R2 state

## Outcome

SEC-04 satisfied. All 1309 photos and thumbnails regenerated with EXIF stripped and re-uploaded to Cloudflare R2 with correct Cache-Control headers. Live R2 verification passes all four ROADMAP success criteria.

## Task Results

### Task 1: Verify Phase 6 code, clear stale outputs, regenerate via resize.py

**Pre-flight verification:**
- `grep -c 'exif=b""' pipeline/resize.py` → **3** (≥ 2 required ✓)
- `grep -c 'max-age=31536000, immutable' pipeline/upload.py` → **2** (≥ 2 required ✓)
- `grep -c 'max-age=86400' pipeline/upload.py` → **1** (≥ 1 required ✓)
- `pipeline/output/metadata.json` SHA256 (pre-resize): `42dfff73080c71502469227e689ae367cbb9c941e7a04c965bde81bf94aef7bf`
- `pipeline/output/catalog.json` present ✓
- `sources/` status: `?? sources/` (untracked, unmodified) ✓

**Stale output cleared:** 1327 × photos + 1327 × thumbs deleted before resize.

**resize.py output:**
```
Resize complete: 1309 photo(s) → pipeline/output/photos/
                 1309 thumb(s) → pipeline/output/thumbs/
Skipped/failed:  0
```
Note: 1309 photos processed (18 fewer than the 1327 previously in output). The pre-Phase-6 output had contained some legacy/duplicate files; the current catalog.json correctly reflects 1309 unique source photos.

**Post-run integrity checks:**
- `ls pipeline/output/photos/*.jpg | wc -l` → **1309** ✓
- `ls pipeline/output/thumbs/*.jpg | wc -l` → **1309** ✓
- `metadata.json` SHA256 post-resize: `42dfff73080c71502469227e689ae367cbb9c941e7a04c965bde81bf94aef7bf` — **matches pre-resize** ✓
- `sources/` post-resize: `?? sources/` — unmodified ✓
- Sample EXIF check on `pipeline/output/photos/abir_sultan_ABR50001.jpg`: `exif_keys: [] / exif empty: True` ✓
- `pipeline/ingest.py`, `pipeline/embed.py`, `pipeline/cluster.py` — **NOT invoked** ✓

### Task 2: Human supplies R2 credentials

R2 credentials (R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY) exported into the execution shell by the user.

### Task 3: Re-upload via upload.py and verify live R2 headers + EXIF stripping

**upload.py output:**
```
Upload complete:
  Photos uploaded:     1309
  Thumbs uploaded:     1309
  metadata.json:       uploaded
  Skipped (security):  0
```

**Cluster integrity (pre-upload vs post-upload cluster hash):**
- Hash: `d5798e4fd7e5363f6459e94bb1a6de77e35ffedccb306bce1aba8de2e22bd2f9`
- Pre == Post: **MATCH** ✓ (upload.py only mutated r2_url/thumb_url fields)

**Live R2 Cache-Control headers** (sample photo: `abir_sultan_ABR50001`):
- Photo: `Cache-Control: public, max-age=31536000, immutable` ✓
- Thumb: `Cache-Control: public, max-age=31536000, immutable` ✓
- metadata.json: `Cache-Control: public, max-age=86400` ✓

**Live R2 EXIF check** (downloaded `abir_sultan_ABR50001.jpg` from R2):
- `dict(Image.open('/tmp/phase8_sample.jpg').getexif())` → `{}` ✓

**Source-tree integrity:** `git status --porcelain sources/` → `?? sources/` (unmodified) ✓

## ROADMAP Success Criteria

| # | Criterion | Result |
|---|-----------|--------|
| 1 | resize.py + upload.py ran; ingest/embed/cluster NOT run | ✓ |
| 2 | Live photo URL returns `Cache-Control: public, max-age=31536000, immutable` | ✓ |
| 3 | Live metadata.json returns `Cache-Control: public, max-age=86400` | ✓ |
| 4 | Downloaded R2 photo has empty `getexif()` (EXIF stripped) | ✓ |

## SEC-04 Disposition

SATISFIED. All 1309 photos and thumbnails on R2 were regenerated by resize.py post-Phase-6 (EXIF stripped), then re-uploaded with correct Cache-Control headers. The v1.1 Security & Hardening milestone is shippable — the gallery URL is safe to share with guests.
