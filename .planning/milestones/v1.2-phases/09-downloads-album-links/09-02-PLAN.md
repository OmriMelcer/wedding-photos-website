---
phase: 09-downloads-album-links
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/PhotoCard.jsx
  - site/tests/PhotoCard.test.jsx
autonomous: true
requirements:
  - DWNL-02
tags:
  - react
  - rtl
  - download

must_haves:
  truths:
    - "A download anchor is present in every PhotoCard"
    - "The download anchor's href equals photo.r2_url"
    - "The download anchor has the HTML `download` attribute set to photo.filename"
    - "Clicking the download anchor does NOT trigger the PhotoCard's onClick (lightbox)"
    - "The download anchor is positioned at bottom-0 end-0 (logical) — visual left in RTL"
    - "The download icon overlay is hidden by default and appears on group hover"
    - "The download anchor has aria-label 'הורד תמונה'"
  artifacts:
    - path: "site/src/components/PhotoCard.jsx"
      provides: "PhotoCard with hover-visible download icon"
      contains: "download="
    - path: "site/tests/PhotoCard.test.jsx"
      provides: "Tests for DWNL-02 behavior"
  key_links:
    - from: "site/src/components/PhotoCard.jsx"
      to: "lucide-react"
      via: "import { Download } from 'lucide-react'"
      pattern: "from ['\"]lucide-react['\"]"
    - from: "PhotoCard download anchor"
      to: "photo.r2_url"
      via: "href + download attributes"
      pattern: "href=\\{photo\\.r2_url\\}"
---

<objective>
Add a hover-visible download icon overlay to `PhotoCard` that downloads the photo from its R2 URL using the native browser download attribute. Satisfies DWNL-02.

Purpose: Guests can save any photo straight from the gallery grid without opening the lightbox, mirroring the photographer credit badge behavior already in PhotoCard.

Output: `site/src/components/PhotoCard.jsx` updated with a download anchor + new `site/tests/PhotoCard.test.jsx`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-downloads-album-links/09-UI-SPEC.md

<interfaces>
<!-- PhotoCard current contract (do not break) -->
From site/src/components/PhotoCard.jsx (current state, 27 lines):
- props: { photo, onClick }
- photo shape (from metadata.json schema in CLAUDE.md): { id, filename, r2_url, thumb_url, photographer, timestamp, cluster, cluster_confidence, faces }
- Currently renders: outer <div className="relative group cursor-pointer overflow-hidden rounded-sm mb-0" onClick={onClick}>
  - <img src={photo.thumb_url} loading="lazy" />
  - photographer credit badge at `absolute bottom-0 start-0` (visual right in RTL), shows on group-hover

<!-- Download anchor contract (NEW) -->
- Element: <a href={photo.r2_url} download={photo.filename} aria-label="הורד תמונה">
- Position: absolute bottom-0 end-0 (visual left in RTL — opposite corner from photographer badge per UI-SPEC §RTL Contract item 3)
- Visibility: opacity-0 by default, group-hover:opacity-100 (mirrors photographer badge)
- Icon: lucide-react `Download`, size 16px (size-4), aria-hidden="true"
- Click behavior: onClick handler calls `e.stopPropagation()` to prevent the parent div's onClick (lightbox open) from firing

<!-- lucide-react import pattern -->
From existing codebase usage and lucide-react ^1.16.0:
- import { Download } from 'lucide-react'
- Renders as <svg>; supports className and aria-hidden props
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write PhotoCard download tests + add hover download overlay</name>
  <files>site/src/components/PhotoCard.jsx, site/tests/PhotoCard.test.jsx</files>
  <read_first>
    - site/src/components/PhotoCard.jsx (current implementation — preserve image + photographer badge, mirror badge positioning for the new overlay)
    - site/tests/Filters.test.jsx (test style for component-level Vitest specs — render + screen + fireEvent)
    - site/tests/Lightbox.test.jsx (pattern for mocking + fireEvent.click + stopPropagation verification)
    - .planning/phases/09-downloads-album-links/09-UI-SPEC.md §"Modified: PhotoCard" and §Accessibility (aria-label "הורד תמונה")
    - site/src/config.js (PHOTOGRAPHER_NAMES — referenced by existing badge, do not break)
  </read_first>
  <behavior>
    - Test: PhotoCard renders an anchor with `href === photo.r2_url`
    - Test: PhotoCard's download anchor has the HTML `download` attribute equal to `photo.filename`
    - Test: PhotoCard's download anchor has `aria-label="הורד תמונה"`
    - Test: clicking the download anchor does NOT invoke the `onClick` prop (stopPropagation works)
    - Test: clicking the image (not the download anchor) DOES invoke the `onClick` prop (lightbox still opens)
    - Test: the download anchor's className contains both `end-0` and `bottom-0` (logical positioning at inline-end, bottom edge)
    - Test: the download anchor's className contains `opacity-0` and `group-hover:opacity-100` (hover-only visibility)
    - Test: no `left-` / `right-` / `ml-` / `mr-` / `pl-` / `pr-` classes anywhere in PhotoCard's rendered output (RTL contract)
  </behavior>
  <action>
    Modify `site/src/components/PhotoCard.jsx`: import `Download` from `lucide-react` (named import, alongside any existing imports). Inside the outer relative-group div, after the existing photographer credit badge, add an `<a>` element with: `href={photo.r2_url}`, `download={photo.filename}`, `aria-label="הורד תמונה"`, `onClick={(e) => e.stopPropagation()}`, and `className="absolute bottom-0 end-0 m-1.5 p-1.5 rounded bg-black/50 text-white opacity-0 group-hover:opacity-100 transition-opacity duration-200 focus-visible:opacity-100"`. Inside the anchor render `<Download className="size-4" aria-hidden="true" />`. Do NOT make the photographer badge interactive — it remains `pointer-events-none`. The download anchor is interactive (no pointer-events-none). Preserve the existing photographer credit badge exactly (className, positioning at start-0, conditional name lookup via PHOTOGRAPHER_NAMES). Create `site/tests/PhotoCard.test.jsx` covering the behaviors listed above. Use `@testing-library/react`, `vitest`, and `fireEvent.click`. Construct a fixture photo via `{ id: 'p1', filename: 'p1.jpg', r2_url: 'https://r2.example.com/photos/p1.jpg', thumb_url: 'https://r2.example.com/thumbs/p1.jpg', photographer: 'abir_sultan', timestamp: '2025-06-14T17:32:00', cluster: 'prep', cluster_confidence: 0.9, faces: [] }`. Use `screen.getByRole('link', { name: 'הורד תמונה' })` to grab the download anchor.
  </action>
  <verify>
    <automated>cd site && npm test -- --run tests/PhotoCard.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import { Download } from 'lucide-react'" site/src/components/PhotoCard.jsx` returns a match (or an equivalent grouped import containing Download)
    - `grep -n "href={photo.r2_url}" site/src/components/PhotoCard.jsx` returns a match
    - `grep -n "download={photo.filename}" site/src/components/PhotoCard.jsx` returns a match
    - `grep -F 'aria-label="הורד תמונה"' site/src/components/PhotoCard.jsx` returns a match
    - `grep -n "e.stopPropagation()" site/src/components/PhotoCard.jsx` returns a match
    - `grep -n "bottom-0 end-0" site/src/components/PhotoCard.jsx` returns a match
    - `grep -n "group-hover:opacity-100" site/src/components/PhotoCard.jsx` returns at least two matches (photographer badge + download anchor)
    - `grep -nE "(^|[^a-z-])(left-|right-|ml-|mr-|pl-|pr-)" site/src/components/PhotoCard.jsx | grep -v '^#'` returns no matches
    - `cd site && npm test -- --run tests/PhotoCard.test.jsx` exits 0 and reports all assertions passing
    - `cd site && npm test -- --run` (full suite) is green — no regressions in Gallery/Lightbox tests
  </acceptance_criteria>
  <done>
    PhotoCard renders a hover-visible download anchor at the bottom-end corner that downloads photo.r2_url with the original filename, does not trigger lightbox open on click, and is fully covered by tests. DWNL-02 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest browser → R2 (image asset) | Browser fetches photo.r2_url directly; R2 bucket is already publicly readable for thumbs and full-size assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Direct R2 URL exposure | accept | photo.r2_url is already exposed by the gallery (img src and lightbox both use it); the download anchor exposes nothing new |
| T-09-05 | Tampering | XSS via photo.filename in download attribute | mitigate | filename comes from pipeline output (server-controlled metadata.json), not user input; React escapes attribute values by default |
| T-09-06 | Denial of Service | Hover icon triggers extra fetches | accept | No extra fetch — the download attribute uses the same URL already loaded as thumb_url's full-size sibling; download only triggers on user click |
</threat_model>

<verification>
- `cd site && npm test -- --run tests/PhotoCard.test.jsx` → all DWNL-02 assertions pass
- `cd site && npm test -- --run` → full suite green (no regression in existing PhotoCard usage via Gallery/GallerySection)
- `cd site && npm run build` → builds cleanly
- Manual (post-execute): hover any gallery card → download icon appears at the visual left bottom corner; click → file downloads with the original filename; clicking the image area still opens the lightbox
</verification>

<success_criteria>
- PhotoCard renders a download anchor with href=photo.r2_url and download=photo.filename (DWNL-02)
- Anchor is at logical bottom-end (visual left in RTL), opposite the photographer badge
- Hover-only visibility via group-hover
- Click on download does NOT open lightbox (stopPropagation)
- aria-label "הורד תמונה" present
- All Vitest specs green; no RTL regressions
</success_criteria>

<output>
Create `.planning/phases/09-downloads-album-links/09-02-SUMMARY.md` when done. Include: files modified, PhotoCard before/after diff highlights, test results, any deviations from UI-SPEC §"Modified: PhotoCard".
</output>
